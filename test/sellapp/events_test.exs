# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.EventsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list_integration_events succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/events")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("events/list_integration_events"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem{
                  id: "evt_00000000000000000000000000",
                  type: "type"
                }
                | _
              ]
            }} = SellApp.Events.list_integration_events(client)
  end

  test "list_integration_events handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Events.list_integration_events(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "list_order_events succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/events")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("events/list_order_events"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem{
                  id: "evt_00000000000000000000000000",
                  type: "type"
                }
                | _
              ]
            }} = SellApp.Events.list_order_events(client, "test_order")
  end

  test "list_order_events handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Events.list_order_events(client, "test_order")

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "list_integration_events encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["cursor"] == "hello world&x"
      refute Map.has_key?(params, "limit")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("events/list_integration_events"))
    end)

    assert {:ok, _} = SellApp.Events.list_integration_events(client, %{cursor: "hello world&x"})
  end

  test "list_integration_events honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("events/list_integration_events"))
    end)

    SellApp.Events.list_integration_events(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.Events.list_integration_events(client)
  end
end
