# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.StoreSettingsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/store/settings")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/get"))
    end)

    assert {:ok, %SellApp.SdkGetStoreSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.get(client)
  end

  test "replace_general succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/general")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/replace_general"))
    end)

    assert {:ok, %SellApp.SdkReplaceGeneralStoreSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.replace_general(client, %{
               "name" => "name",
               "visibility" => "PUBLIC",
               "timezone" => "timezone",
               "currency" => "cur",
               "dark_mode" => nil
             })
  end

  test "update_general succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/general")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/update_general"))
    end)

    assert {:ok, %SellApp.SdkUpdateGeneralStoreSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.update_general(client, %{
               "name" => "name",
               "visibility" => "PUBLIC",
               "timezone" => "timezone",
               "currency" => "cur",
               "dark_mode" => nil
             })
  end

  test "replace_analytics succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/analytics")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/replace_analytics"))
    end)

    assert {:ok, %SellApp.SdkReplaceAnalyticsSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.replace_analytics(client, %{
               "ga4_measurement_id" => nil,
               "meta_pixel_id" => nil,
               "tiktok_pixel_id" => nil
             })
  end

  test "update_analytics succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/analytics")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/update_analytics"))
    end)

    assert {:ok, %SellApp.SdkUpdateAnalyticsSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.update_analytics(client, %{
               "ga4_measurement_id" => nil,
               "meta_pixel_id" => nil,
               "tiktok_pixel_id" => nil
             })
  end

  test "replace_marketing succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/marketing")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/replace_marketing"))
    end)

    assert {:ok, %SellApp.SdkReplaceMarketingSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.replace_marketing(client, %{
               "abandoned_cart" => %{
                 "enabled" => true,
                 "min_total_cents" => nil,
                 "max_total_cents" => nil,
                 "reminders" => [%{"after_hours" => nil}],
                 "incentive_mode" => nil,
                 "coupon_id" => nil,
                 "send_incentive_on_step" => nil
               }
             })
  end

  test "update_marketing succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/store/settings/marketing")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/update_marketing"))
    end)

    assert {:ok, %SellApp.SdkUpdateMarketingSettingsResponseValue200ApplicationJson{}} =
             SellApp.StoreSettings.update_marketing(client, %{
               "abandoned_cart" => %{
                 "enabled" => true,
                 "min_total_cents" => nil,
                 "max_total_cents" => nil,
                 "reminders" => [%{"after_hours" => nil}],
                 "incentive_mode" => nil,
                 "coupon_id" => nil,
                 "send_incentive_on_step" => nil
               }
             })
  end

  test "get honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_settings/get"))
    end)

    SellApp.StoreSettings.get(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.StoreSettings.get(client)
  end
end
