# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.ChargesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/charges")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListChargesResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  url: "url"
                }
                | _
              ]
            }} = SellApp.Charges.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Charges.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/charges")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/create"))
    end)

    assert {:ok, %SellApp.SdkCreateChargeResponseValue201ApplicationJson{}} =
             SellApp.Charges.create(client, %{
               "email" => "email",
               "return_url" => "return_url",
               "cancel_url" => nil,
               "webhook" => nil,
               "reference" => nil,
               "description" => nil,
               "currency" => "cur",
               "total" => 1,
               "payment_method" => "AUTHNET",
               "payment_methods" => ["AUTHNET"],
               "custom_payment_method_id" => nil,
               "custom_payment_method_ids" => ["custom_payment_method_idsx"],
               "use_all_payment_methods" => true,
               "deliverable" => %{},
               "metadata" => %{},
               "coupon_code" => nil
             })
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/charges/test_charge")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/get"))
    end)

    assert {:ok, %SellApp.SdkGetChargeResponseValue200ApplicationJson{}} =
             SellApp.Charges.get(client, "test_charge")
  end

  test "mark_completed succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/charges/test_charge_id/completed")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/mark_completed"))
    end)

    assert {:ok,
            %SellApp.SdkMarkPendingChargeCompletedResponseValue200ApplicationJson{
              message: "message"
            }} = SellApp.Charges.mark_completed(client, "test_charge_id")
  end

  test "mark_voided succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/charges/test_charge_id/voided")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/mark_voided"))
    end)

    assert {:ok,
            %SellApp.SdkMarkPendingChargeVoidedResponseValue200ApplicationJson{message: "message"}} =
             SellApp.Charges.mark_voided(client, "test_charge_id")
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/list"))
    end)

    assert {:ok, _} = SellApp.Charges.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("charges/list"))
    end)

    SellApp.Charges.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Charges.list(client)
  end
end
