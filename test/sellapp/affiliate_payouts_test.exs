# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.AffiliatePayoutsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-payouts")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  affiliate_id: 1
                }
                | _
              ]
            }} = SellApp.AffiliatePayouts.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.AffiliatePayouts.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-payouts/test_payout")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/get"))
    end)

    assert {:ok, %SellApp.SdkGetAffiliatePayoutResponseValue200ApplicationJson{}} =
             SellApp.AffiliatePayouts.get(client, "test_payout")
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/affiliates/test_affiliate/payouts")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/create"))
    end)

    assert {:ok, %SellApp.SdkCreateAffiliatePayoutResponseValue200ApplicationJson{}} =
             SellApp.AffiliatePayouts.create(client, "test_affiliate")
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-payouts/test_payout/status")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson{}} =
             SellApp.AffiliatePayouts.update(client, "test_payout", %{"status" => "due"})
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["affiliate_id"] == "hello world&x"
      refute Map.has_key?(params, "limit")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/list"))
    end)

    assert {:ok, _} = SellApp.AffiliatePayouts.list(client, %{affiliate_id: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_payouts/list"))
    end)

    SellApp.AffiliatePayouts.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.AffiliatePayouts.list(client)
  end
end
