# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.InvoicesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/invoices")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListInvoicesResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  created_at: "2024-01-01T00:00:00.000Z"
                }
                | _
              ]
            }} = SellApp.Invoices.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Invoices.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/invoices")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/create"))
    end)

    assert {:ok, %SellApp.SdkCreateInvoiceResponseValue201ApplicationJson{}} =
             SellApp.Invoices.create(client, %{
               "customer_email" => "customer_email",
               "customer_ip" => nil,
               "payment_method" => "AUTHNET",
               "coupon" => "coupon",
               "vat_id" => "vat_id",
               "country" => "country",
               "affiliate" => nil,
               "extra" => %{"amount" => 1, "currency" => "currency"},
               "product_variants" => %{}
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/invoices/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchInvoicesResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  created_at: "2024-01-01T00:00:00.000Z"
                }
                | _
              ]
            }} =
             SellApp.Invoices.search(client, %{
               "search" => "search",
               "search_by" => "default",
               "id" => "id",
               "email" => "email",
               "transaction_id" => "transaction_id",
               "serial_code" => "serial_code",
               "additional_info" => "additional_info",
               "product_name" => "product_name",
               "discord_data" => "discord_data",
               "crypto_txid" => "crypto_txid",
               "crypto_address" => "crypto_address",
               "coupon_code" => "coupon_code",
               "status" => ["PENDING"],
               "payment_methods" => ["AUTHNET"],
               "sort" => "created_at"
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Invoices.search(client, %{
               "search" => "search",
               "search_by" => "default",
               "id" => "id",
               "email" => "email",
               "transaction_id" => "transaction_id",
               "serial_code" => "serial_code",
               "additional_info" => "additional_info",
               "product_name" => "product_name",
               "discord_data" => "discord_data",
               "crypto_txid" => "crypto_txid",
               "crypto_address" => "crypto_address",
               "coupon_code" => "coupon_code",
               "status" => ["PENDING"],
               "payment_methods" => ["AUTHNET"],
               "sort" => "created_at"
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/get"))
    end)

    assert {:ok, %SellApp.SdkGetInvoiceResponseValue200ApplicationJson{}} =
             SellApp.Invoices.get(client, "test_invoice")
  end

  test "go_to_checkout succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/checkout")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/go_to_checkout"))
    end)

    assert {:ok,
            %SellApp.SdkCreateCheckoutSessionResponseValue200ApplicationJson{
              message: "message",
              payment_url: "payment_url"
            }} = SellApp.Invoices.go_to_checkout(client, "test_invoice")
  end

  test "get_deliverables succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/deliverables")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/get_deliverables"))
    end)

    assert {:ok, %SellApp.SdkGetInvoiceDeliverablesResponseValue200ApplicationJson{}} =
             SellApp.Invoices.get_deliverables(client, "test_invoice")
  end

  test "mark_completed succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/mark-completed")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/mark_completed"))
    end)

    assert {:ok,
            %SellApp.SdkMarkPendingInvoiceCompletedResponseValue200ApplicationJson{
              id: 1,
              created_at: "2024-01-01T00:00:00.000Z"
            }} =
             SellApp.Invoices.mark_completed(client, "test_invoice", %{"expected_status" => nil})
  end

  test "mark_voided succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/mark-voided")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/mark_voided"))
    end)

    assert {:ok,
            %SellApp.SdkMarkPendingInvoiceVoidedResponseValue200ApplicationJson{
              id: 1,
              created_at: "2024-01-01T00:00:00.000Z"
            }} = SellApp.Invoices.mark_voided(client, "test_invoice", %{"expected_status" => nil})
  end

  test "issue_replacement succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/issue-replacement")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/issue_replacement"))
    end)

    assert {:ok,
            %SellApp.SdkIssueReplacementForCompletedInvoiceResponseValue200ApplicationJson{
              id: 1,
              created_at: "2024-01-01T00:00:00.000Z"
            }} =
             SellApp.Invoices.issue_replacement(client, "test_invoice", %{
               "product_variants" => %{}
             })
  end

  test "update_status succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/status")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/update_status"))
    end)

    assert {:ok,
            %SellApp.SdkUpdateInvoiceStatusResponseValue200ApplicationJson{
              id: 1,
              created_at: "2024-01-01T00:00:00.000Z"
            }} =
             SellApp.Invoices.update_status(client, "test_invoice", %{
               "status" => "COMPLETED",
               "expected_status" => nil
             })
  end

  test "create_refund succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/refunds")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/create_refund"))
    end)

    assert {:ok, %SellApp.SdkCreateInvoiceRefundResponseValue200ApplicationJson{}} =
             SellApp.Invoices.create_refund(client, "test_invoice", %{"amount" => nil})
  end

  test "retry_fulfillment succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/invoices/test_invoice/fulfillment-retries")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/retry_fulfillment"))
    end)

    assert {:ok, %SellApp.SdkCreateFulfillmentRetryResponseValue200ApplicationJson{}} =
             SellApp.Invoices.retry_fulfillment(client, "test_invoice", %{"email" => nil})
  end

  test "retry_dynamic_delivery succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/invoices/test_invoice/dynamic-delivery-retries"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/retry_dynamic_delivery"))
    end)

    assert {:ok, %SellApp.SdkCreateDynamicDeliveryRetryResponseValue200ApplicationJson{}} =
             SellApp.Invoices.retry_dynamic_delivery(client, "test_invoice", %{
               "delivered_product_id" => 1
             })
  end

  test "notify_fulfillment succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/invoices/test_invoice/fulfillment-notifications"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/notify_fulfillment"))
    end)

    assert {:ok, %SellApp.SdkCreateFulfillmentNotificationsResponseValue200ApplicationJson{}} =
             SellApp.Invoices.notify_fulfillment(client, "test_invoice", %{
               "email" => nil,
               "product_variant_ids" => nil
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      assert params["status"] == "a,b"
      assert params["payment_methods"] == "a,b"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/list"))
    end)

    assert {:ok, _} =
             SellApp.Invoices.list(client, %{
               limit: "hello world&x",
               status: ["a", "b"],
               payment_methods: ["a", "b"]
             })
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("invoices/list"))
    end)

    SellApp.Invoices.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Invoices.list(client)
  end
end
