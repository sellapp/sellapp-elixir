# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.HighlightsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/highlights")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/list"))
    end)

    assert {:ok,
            %SellApp.Page{data: [%SellApp.SdkListHighlightsResponseValue200ApplicationJson{} | _]}} =
             SellApp.Highlights.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Highlights.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/highlights")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/create"))
    end)

    assert {:ok, %SellApp.SdkCreateHighlightResponseValue201ApplicationJson{}} =
             SellApp.Highlights.create(client, %{
               "title" => "title",
               "hidden" => true,
               "files" => ["binary"]
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/highlights/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkSearchHighlightsResponseValue200ApplicationJson{} | _]
            }} =
             SellApp.Highlights.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Highlights.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "reorder succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/highlights/reorder")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/reorder"))
    end)

    assert {:ok, %SellApp.SdkReorderHighlightsResponseValue200ApplicationJson{}} =
             SellApp.Highlights.reorder(client, %{"resources" => [1]})
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/get"))
    end)

    assert {:ok, %SellApp.SdkGetHighlightResponseValue200ApplicationJson{}} =
             SellApp.Highlights.get(client, "test_highlight")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceHighlightResponseValue200ApplicationJson{}} =
             SellApp.Highlights.replace(client, "test_highlight", %{
               "title" => "title",
               "hidden" => true,
               "published" => true
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateHighlightResponseValue200ApplicationJson{}} =
             SellApp.Highlights.update(client, "test_highlight", %{
               "title" => "title",
               "hidden" => true,
               "published" => true
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteHighlightResponseValue200ApplicationJson{}} =
             SellApp.Highlights.delete(client, "test_highlight")
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/list"))
    end)

    assert {:ok, _} = SellApp.Highlights.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights/list"))
    end)

    SellApp.Highlights.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Highlights.list(client)
  end
end
