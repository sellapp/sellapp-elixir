# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.AffiliateProgramTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-program")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/get"))
    end)

    assert {:ok, %SellApp.SdkGetAffiliateProgramConfigurationResponseValue200ApplicationJson{}} =
             SellApp.AffiliateProgram.get(client)
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-program")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/replace"))
    end)

    assert {:ok,
            %SellApp.SdkReplaceAffiliateProgramConfigurationResponseValue200ApplicationJson{}} =
             SellApp.AffiliateProgram.replace(client, %{
               "enabled" => true,
               "settings" => %{
                 "auto_approve_affiliates" => true,
                 "minimum_payout" => "minimum_payout",
                 "commission" => %{"type" => "percentage", "amount" => "amount"},
                 "referrer_type" => "first_referrer",
                 "tracking_length" => 1,
                 "subscription_commission" => true,
                 "enabled_specific_products" => true,
                 "payout_methods" => ["PAYPAL"],
                 "enable_hub" => true
               },
               "products" => [%{"id" => 1, "enabled" => true, "commission" => %{}}]
             })
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-invitations")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListPendingAffiliateInvitationsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  email: "email"
                }
                | _
              ]
            }} = SellApp.AffiliateProgram.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.AffiliateProgram.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "invite succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/affiliate-invitations")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/invite"))
    end)

    assert {:ok, %SellApp.SdkInviteAffiliateResponseValue201ApplicationJson{}} =
             SellApp.AffiliateProgram.invite(client, %{"email" => "email"})
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/list"))
    end)

    assert {:ok, _} = SellApp.AffiliateProgram.list(client, %{limit: "hello world&x"})
  end

  test "get honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("affiliate_program/get"))
    end)

    SellApp.AffiliateProgram.get(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.AffiliateProgram.get(client)
  end
end
