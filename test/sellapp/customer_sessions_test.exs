# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CustomerSessionsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "create_customer_session succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/customer-sessions")

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("customer_sessions/create_customer_session")
      )
    end)

    assert {:ok, %SellApp.SdkCreateCustomerSessionResponseValue201ApplicationJson{}} =
             SellApp.CustomerSessions.create_customer_session(client, %{"customer_id" => 1})
  end

  test "revoke_customer_session succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/customer-sessions/test_session")

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("customer_sessions/revoke_customer_session")
      )
    end)

    assert {:ok, %SellApp.SdkRevokeCustomerSessionResponseValue200ApplicationJson{revoked: true}} =
             SellApp.CustomerSessions.revoke_customer_session(client, "test_session")
  end

  test "create_customer_session honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("customer_sessions/create_customer_session")
      )
    end)

    SellApp.CustomerSessions.create_customer_session(
      client,
      %{"customer_id" => 1},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.CustomerSessions.create_customer_session(client, %{"customer_id" => 1})
  end
end
