# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.HighlightsMediaTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight/media")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/list"))
    end)

    assert {:ok, %SellApp.SdkListHighlightMediaResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.list(client, "test_highlight")
  end

  test "add succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight/media")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/add"))
    end)

    assert {:ok, %SellApp.SdkAddHighlightMediaResponseValue201ApplicationJson{}} =
             SellApp.HighlightsMedia.add(client, "test_highlight", %{
               "file" => "binary",
               "cta_title" => "cta_title",
               "product_id" => nil
             })
  end

  test "reorder succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/highlights/test_highlight/media/reorder")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/reorder"))
    end)

    assert {:ok, %SellApp.SdkReorderHighlightMediaResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.reorder(client, "test_highlight", %{"resources" => [1]})
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/highlights/test_highlight/media/test_media"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceHighlightMediaResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.replace(client, "test_highlight", "test_media", %{
               "file" => "binary",
               "cta_title" => "cta_title",
               "product_id" => nil
             })
  end

  test "replace_metadata succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/highlights/test_highlight/media/test_media"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/replace_metadata"))
    end)

    assert {:ok, %SellApp.SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.replace_metadata(client, "test_highlight", "test_media", %{
               "cta_title" => "cta_title",
               "product_id" => nil
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"

      assert String.ends_with?(
               conn.request_path,
               "/v2/highlights/test_highlight/media/test_media"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateHighlightMediaResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.update(client, "test_highlight", "test_media", %{
               "cta_title" => "cta_title",
               "product_id" => nil
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"

      assert String.ends_with?(
               conn.request_path,
               "/v2/highlights/test_highlight/media/test_media"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteHighlightMediaResponseValue200ApplicationJson{}} =
             SellApp.HighlightsMedia.delete(client, "test_highlight", "test_media")
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("highlights_media/list"))
    end)

    SellApp.HighlightsMedia.list(
      client,
      "test_highlight",
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.HighlightsMedia.list(client, "test_highlight")
  end
end
