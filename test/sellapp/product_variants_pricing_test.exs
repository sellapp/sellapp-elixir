# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.ProductVariantsPricingTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/pricing"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("product_variants_pricing/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceProductVariantPricingResponseValue200ApplicationJson{}} =
             SellApp.ProductVariantsPricing.replace(client, "test_product", "test_variant", %{
               "pricing" => %{
                 "type" => "SINGLE_PAYMENT",
                 "price" => %{"price" => 1, "currency" => "AUD"},
                 "compare_at_price" => nil,
                 "humble" => true,
                 "frequency" => %{"value" => 1, "interval" => "DAY"}
               },
               "payment_methods" => ["AUTHNET"],
               "custom_payment_method_ids" => ["00000000000000000000000000"],
               "bulk_discount" => [%{"minimum_purchase_amount" => 1, "discount_percentage" => 1}],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/pricing"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("product_variants_pricing/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateProductVariantPricingResponseValue200ApplicationJson{}} =
             SellApp.ProductVariantsPricing.update(client, "test_product", "test_variant", %{
               "pricing" => %{
                 "type" => "SINGLE_PAYMENT",
                 "price" => %{"price" => 1, "currency" => "AUD"},
                 "compare_at_price" => nil,
                 "humble" => true,
                 "frequency" => %{"value" => 1, "interval" => "DAY"}
               },
               "payment_methods" => ["AUTHNET"],
               "custom_payment_method_ids" => ["00000000000000000000000000"],
               "bulk_discount" => [%{"minimum_purchase_amount" => 1, "discount_percentage" => 1}],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "replace honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("product_variants_pricing/replace"))
    end)

    SellApp.ProductVariantsPricing.replace(
      client,
      "test_product",
      "test_variant",
      %{
        "pricing" => %{
          "type" => "SINGLE_PAYMENT",
          "price" => %{"price" => 1, "currency" => "AUD"},
          "compare_at_price" => nil,
          "humble" => true,
          "frequency" => %{"value" => 1, "interval" => "DAY"}
        },
        "payment_methods" => ["AUTHNET"],
        "custom_payment_method_ids" => ["00000000000000000000000000"],
        "bulk_discount" => [%{"minimum_purchase_amount" => 1, "discount_percentage" => 1}],
        "expected_updated_at" => "2024-01-01T00:00:00.000Z"
      },
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.ProductVariantsPricing.replace(client, "test_product", "test_variant", %{
               "pricing" => %{
                 "type" => "SINGLE_PAYMENT",
                 "price" => %{"price" => 1, "currency" => "AUD"},
                 "compare_at_price" => nil,
                 "humble" => true,
                 "frequency" => %{"value" => 1, "interval" => "DAY"}
               },
               "payment_methods" => ["AUTHNET"],
               "custom_payment_method_ids" => ["00000000000000000000000000"],
               "bulk_discount" => [%{"minimum_purchase_amount" => 1, "discount_percentage" => 1}],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end
end
