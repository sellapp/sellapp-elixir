# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CreditsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "record succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/credit-transactions")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("credits/record"))
    end)

    assert {:ok, %SellApp.SdkRecordCreditTransactionResponseValue200ApplicationJson{}} =
             SellApp.Credits.record(client, %{
               "customer_id" => 1,
               "product_id" => 1,
               "kind" => "grant",
               "amount_units" => 1,
               "idempotency_key" => "idempotency_key",
               "reason" => nil,
               "source_type" => nil,
               "source_id" => nil,
               "metadata" => %{}
             })
  end

  test "record honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("credits/record"))
    end)

    SellApp.Credits.record(
      client,
      %{
        "customer_id" => 1,
        "product_id" => 1,
        "kind" => "grant",
        "amount_units" => 1,
        "idempotency_key" => "idempotency_key",
        "reason" => nil,
        "source_type" => nil,
        "source_id" => nil,
        "metadata" => %{}
      },
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.Credits.record(client, %{
               "customer_id" => 1,
               "product_id" => 1,
               "kind" => "grant",
               "amount_units" => 1,
               "idempotency_key" => "idempotency_key",
               "reason" => nil,
               "source_type" => nil,
               "source_id" => nil,
               "metadata" => %{}
             })
  end
end
