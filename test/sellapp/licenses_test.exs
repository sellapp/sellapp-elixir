# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.LicensesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "activate succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/licenses/activate")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/activate"))
    end)

    assert {:ok, %SellApp.SdkActivateLicenseKeyResponseValue200ApplicationJson{}} =
             SellApp.Licenses.activate(client, %{
               "license_key" => "license_key",
               "instance_name" => "instance_name"
             })
  end

  test "validate succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/licenses/validate")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/validate"))
    end)

    assert {:ok, %SellApp.SdkValidateLicenseKeyResponseValue200ApplicationJson{}} =
             SellApp.Licenses.validate(client, %{
               "license_key" => "license_key",
               "instance_id" => "instance_id"
             })
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/license-keys")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkListLicenseKeysResponseValue200ApplicationJson{} | _]
            }} = SellApp.Licenses.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Licenses.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/license-keys/test_license_key")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/get"))
    end)

    assert {:ok, %SellApp.SdkGetLicenseKeyResponseValue200ApplicationJson{}} =
             SellApp.Licenses.get(client, "test_license_key")
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/license-keys/test_license_key")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateLicenseKeyResponseValue200ApplicationJson{}} =
             SellApp.Licenses.update(client, "test_license_key", %{
               "limit" => nil,
               "expires_at" => nil,
               "active" => nil
             })
  end

  test "deactivate_license succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/licenses/deactivate")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/deactivate_license"))
    end)

    assert {:ok, %SellApp.SdkDeactivateLicenseResponseValue200ApplicationJson{}} =
             SellApp.Licenses.deactivate_license(client, %{
               "license_key" => "license_key",
               "instance_id" => "instance_id"
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/list"))
    end)

    assert {:ok, _} = SellApp.Licenses.list(client, %{limit: "hello world&x"})
  end

  test "activate honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("licenses/activate"))
    end)

    SellApp.Licenses.activate(
      client,
      %{"license_key" => "license_key", "instance_name" => "instance_name"},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.Licenses.activate(client, %{
               "license_key" => "license_key",
               "instance_name" => "instance_name"
             })
  end
end
