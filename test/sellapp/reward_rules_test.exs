# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.RewardRulesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkListRewardRulesResponseValue200ApplicationJson{} | _]
            }} = SellApp.RewardRules.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.RewardRules.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/create"))
    end)

    assert {:ok, %SellApp.SdkCreateRewardRuleResponseValue201ApplicationJson{}} =
             SellApp.RewardRules.create(client, %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "trigger_type" => "PURCHASE_COUNT",
               "trigger_threshold" => 1,
               "outputs" => [
                 %{
                   "type" => "WALLET_CREDIT",
                   "amount_cents" => 1,
                   "reward_coupon_template_id" => 1,
                   "label" => nil,
                   "color" => "zinc"
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkSearchRewardRulesResponseValue200ApplicationJson{} | _]
            }} =
             SellApp.RewardRules.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.RewardRules.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules/test_reward_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/get"))
    end)

    assert {:ok, %SellApp.SdkGetRewardRuleResponseValue200ApplicationJson{}} =
             SellApp.RewardRules.get(client, "test_reward_rule")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules/test_reward_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceRewardRuleResponseValue200ApplicationJson{}} =
             SellApp.RewardRules.replace(client, "test_reward_rule", %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "trigger_type" => "PURCHASE_COUNT",
               "trigger_threshold" => 1,
               "outputs" => [
                 %{
                   "type" => "WALLET_CREDIT",
                   "amount_cents" => 1,
                   "reward_coupon_template_id" => 1,
                   "label" => nil,
                   "color" => "zinc"
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/reward-rules/test_reward_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateRewardRuleResponseValue200ApplicationJson{}} =
             SellApp.RewardRules.update(client, "test_reward_rule", %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "trigger_type" => "PURCHASE_COUNT",
               "trigger_threshold" => 1,
               "outputs" => [
                 %{
                   "type" => "WALLET_CREDIT",
                   "amount_cents" => 1,
                   "reward_coupon_template_id" => 1,
                   "label" => nil,
                   "color" => "zinc"
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/list"))
    end)

    assert {:ok, _} = SellApp.RewardRules.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_rules/list"))
    end)

    SellApp.RewardRules.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.RewardRules.list(client)
  end
end
