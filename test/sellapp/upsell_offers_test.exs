# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.UpsellOffersTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkListUpsellOffersResponseValue200ApplicationJson{} | _]
            }} = SellApp.UpsellOffers.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.UpsellOffers.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/create"))
    end)

    assert {:ok, %SellApp.SdkCreateUpsellOfferResponseValue201ApplicationJson{}} =
             SellApp.UpsellOffers.create(client, %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "source_listing_id" => 1,
               "source_variant_id" => nil,
               "minimum_order_total_usd_cents" => nil,
               "maximum_order_total_usd_cents" => nil,
               "starts_at" => nil,
               "ends_at" => nil,
               "available_for_days" => nil,
               "max_accepts_per_customer" => nil,
               "expected_version" => 1,
               "items" => [
                 %{
                   "id" => nil,
                   "target_listing_id" => 1,
                   "target_variant_id" => 1,
                   "headline" => nil,
                   "description" => nil,
                   "discount_type" => nil,
                   "discount_value" => nil,
                   "maximum_discount_amount" => nil
                 }
               ]
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkSearchUpsellOffersResponseValue200ApplicationJson{} | _]
            }} =
             SellApp.UpsellOffers.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.UpsellOffers.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers/test_upsell_offer")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/get"))
    end)

    assert {:ok, %SellApp.SdkGetUpsellOfferResponseValue200ApplicationJson{}} =
             SellApp.UpsellOffers.get(client, "test_upsell_offer")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers/test_upsell_offer")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceUpsellOfferResponseValue200ApplicationJson{}} =
             SellApp.UpsellOffers.replace(client, "test_upsell_offer", %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "source_listing_id" => 1,
               "source_variant_id" => nil,
               "minimum_order_total_usd_cents" => nil,
               "maximum_order_total_usd_cents" => nil,
               "starts_at" => nil,
               "ends_at" => nil,
               "available_for_days" => nil,
               "max_accepts_per_customer" => nil,
               "expected_version" => 1,
               "items" => [
                 %{
                   "id" => nil,
                   "target_listing_id" => 1,
                   "target_variant_id" => 1,
                   "headline" => nil,
                   "description" => nil,
                   "discount_type" => nil,
                   "discount_value" => nil,
                   "maximum_discount_amount" => nil
                 }
               ]
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers/test_upsell_offer")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateUpsellOfferResponseValue200ApplicationJson{}} =
             SellApp.UpsellOffers.update(client, "test_upsell_offer", %{
               "name" => "name",
               "description" => nil,
               "is_active" => true,
               "source_listing_id" => 1,
               "source_variant_id" => nil,
               "minimum_order_total_usd_cents" => nil,
               "maximum_order_total_usd_cents" => nil,
               "starts_at" => nil,
               "ends_at" => nil,
               "available_for_days" => nil,
               "max_accepts_per_customer" => nil,
               "expected_version" => 1,
               "items" => [
                 %{
                   "id" => nil,
                   "target_listing_id" => 1,
                   "target_variant_id" => 1,
                   "headline" => nil,
                   "description" => nil,
                   "discount_type" => nil,
                   "discount_value" => nil,
                   "maximum_discount_amount" => nil
                 }
               ]
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/upsell-offers/test_upsell_offer")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteUpsellOfferResponseValue200ApplicationJson{}} =
             SellApp.UpsellOffers.delete(client, "test_upsell_offer")
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/list"))
    end)

    assert {:ok, _} = SellApp.UpsellOffers.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("upsell_offers/list"))
    end)

    SellApp.UpsellOffers.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.UpsellOffers.list(client)
  end
end
