# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CouponsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/coupons")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListCouponsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  code: "code"
                }
                | _
              ]
            }} = SellApp.Coupons.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Coupons.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v1/coupons")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/create"))
    end)

    assert {:ok, %SellApp.SdkCreateCouponResponseValue201ApplicationJson{}} =
             SellApp.Coupons.create(client, %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/get"))
    end)

    assert {:ok, %SellApp.SdkGetCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.get(client, "test_coupon")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v1/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.replace(client, "test_coupon", %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v1/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.update(client, "test_coupon", %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v1/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.delete(client, "test_coupon")
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v1/coupons/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchCouponsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  code: "code"
                }
                | _
              ]
            }} =
             SellApp.Coupons.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Coupons.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "batch_create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v1/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/batch_create"))
    end)

    assert {:ok, %SellApp.SdkBatchCreateCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.batch_create(client, %{
               "resources" => [
                 %{
                   "code" => "code",
                   "type" => "PERCENTAGE",
                   "discount" => 1,
                   "store_wide" => true,
                   "products" => [1],
                   "product_variants" => [1],
                   "limit" => nil,
                   "expires_at" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "batch_update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v1/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/batch_update"))
    end)

    assert {:ok, %SellApp.SdkBatchUpdateCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.batch_update(client, %{"resources" => %{}})
  end

  test "batch_delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v1/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/batch_delete"))
    end)

    assert {:ok, %SellApp.SdkBatchDeleteCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.batch_delete(client, %{"resources" => [1]})
  end

  test "v2_list_coupons succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/coupons")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_list_coupons"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  code: "code"
                }
                | _
              ]
            }} = SellApp.Coupons.v2_list_coupons(client)
  end

  test "v2_list_coupons handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Coupons.v2_list_coupons(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_create_coupon succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/coupons")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_create_coupon"))
    end)

    assert {:ok, %SellApp.SdkV2CreateCouponResponseValue201ApplicationJson{}} =
             SellApp.Coupons.v2_create_coupon(client, %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "v2_search_coupons succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/coupons/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_search_coupons"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  code: "code"
                }
                | _
              ]
            }} =
             SellApp.Coupons.v2_search_coupons(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "v2_search_coupons handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Coupons.v2_search_coupons(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_get_coupon succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_get_coupon"))
    end)

    assert {:ok, %SellApp.SdkV2GetCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_get_coupon(client, "test_coupon")
  end

  test "v2_replace_coupon succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_replace_coupon"))
    end)

    assert {:ok, %SellApp.SdkV2ReplaceCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_replace_coupon(client, "test_coupon", %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "v2_update_coupon succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_update_coupon"))
    end)

    assert {:ok, %SellApp.SdkV2UpdateCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_update_coupon(client, "test_coupon", %{
               "code" => "code",
               "type" => "PERCENTAGE",
               "discount" => 1,
               "store_wide" => true,
               "products" => [1],
               "product_variants" => [1],
               "limit" => nil,
               "expires_at" => nil,
               "minimum_amount" => nil
             })
  end

  test "v2_delete_coupon succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/coupons/test_coupon")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_delete_coupon"))
    end)

    assert {:ok, %SellApp.SdkV2DeleteCouponResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_delete_coupon(client, "test_coupon")
  end

  test "v2_batch_create_coupons succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_batch_create_coupons"))
    end)

    assert {:ok, %SellApp.SdkV2BatchCreateCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_batch_create_coupons(client, %{
               "resources" => [
                 %{
                   "code" => "code",
                   "type" => "PERCENTAGE",
                   "discount" => 1,
                   "store_wide" => true,
                   "products" => [1],
                   "product_variants" => [1],
                   "limit" => nil,
                   "expires_at" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "v2_batch_update_coupons succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_batch_update_coupons"))
    end)

    assert {:ok, %SellApp.SdkV2BatchUpdateCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_batch_update_coupons(client, %{"resources" => %{}})
  end

  test "v2_batch_delete_coupons succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/coupons/batch")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/v2_batch_delete_coupons"))
    end)

    assert {:ok, %SellApp.SdkV2BatchDeleteCouponsResponseValue200ApplicationJson{}} =
             SellApp.Coupons.v2_batch_delete_coupons(client, %{"resources" => [1]})
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/list"))
    end)

    assert {:ok, _} = SellApp.Coupons.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("coupons/list"))
    end)

    SellApp.Coupons.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Coupons.list(client)
  end
end
