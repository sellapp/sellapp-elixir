# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WalletBonusTiersTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/wallet-bonus-tiers")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/list"))
    end)

    assert {:ok, %SellApp.SdkListWalletBonusTiersResponseValue200ApplicationJson{}} =
             SellApp.WalletBonusTiers.list(client)
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/wallet-bonus-tiers")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/create"))
    end)

    assert {:ok, %SellApp.SdkCreateWalletBonusTierResponseValue201ApplicationJson{}} =
             SellApp.WalletBonusTiers.create(client, %{
               "minimum_top_up_cents" => 1,
               "bonus_kind" => "fixed",
               "fixed_bonus_cents" => nil,
               "percent_basis" => nil,
               "maximum_bonus_cents" => nil,
               "priority" => 1,
               "is_active" => true
             })
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/wallet-bonus-tiers/test_bonus_tier")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceWalletBonusTierResponseValue200ApplicationJson{}} =
             SellApp.WalletBonusTiers.replace(client, "test_bonus_tier", %{
               "minimum_top_up_cents" => 1,
               "bonus_kind" => "fixed",
               "fixed_bonus_cents" => nil,
               "percent_basis" => nil,
               "maximum_bonus_cents" => nil,
               "priority" => 1,
               "is_active" => true
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/wallet-bonus-tiers/test_bonus_tier")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateWalletBonusTierResponseValue200ApplicationJson{}} =
             SellApp.WalletBonusTiers.update(client, "test_bonus_tier", %{
               "minimum_top_up_cents" => 1,
               "bonus_kind" => "fixed",
               "fixed_bonus_cents" => nil,
               "percent_basis" => nil,
               "maximum_bonus_cents" => nil,
               "priority" => 1,
               "is_active" => true
             })
  end

  test "archive succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/wallet-bonus-tiers/test_bonus_tier")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/archive"))
    end)

    assert {:ok, %SellApp.SdkArchiveWalletBonusTierResponseValue200ApplicationJson{}} =
             SellApp.WalletBonusTiers.archive(client, "test_bonus_tier")
  end

  test "restore succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/wallet-bonus-tiers/test_bonus_tier/restore"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/restore"))
    end)

    assert {:ok, %SellApp.SdkRestoreWalletBonusTierResponseValue200ApplicationJson{}} =
             SellApp.WalletBonusTiers.restore(client, "test_bonus_tier")
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_bonus_tiers/list"))
    end)

    SellApp.WalletBonusTiers.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.WalletBonusTiers.list(client)
  end
end
