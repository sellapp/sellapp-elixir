# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.FeedbackTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListFeedbackResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  feedback: "feedback"
                }
                | _
              ]
            }} = SellApp.Feedback.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Feedback.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/feedback/test_feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/get"))
    end)

    assert {:ok, %SellApp.SdkGetFeedbackResponseValue200ApplicationJson{}} =
             SellApp.Feedback.get(client, "test_feedback")
  end

  test "reply succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v1/feedback/test_feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/reply"))
    end)

    assert {:ok, %SellApp.SdkReplyToFeedbackResponseValue200ApplicationJson{}} =
             SellApp.Feedback.reply(client, "test_feedback", %{"reply" => "reply"})
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v1/feedback/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  feedback: "feedback"
                }
                | _
              ]
            }} =
             SellApp.Feedback.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Feedback.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_list_feedback succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/v2_list_feedback"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2ListFeedbackResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  feedback: "feedback"
                }
                | _
              ]
            }} = SellApp.Feedback.v2_list_feedback(client)
  end

  test "v2_list_feedback handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Feedback.v2_list_feedback(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_search_feedback succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/feedback/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/v2_search_feedback"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2SearchFeedbackResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  feedback: "feedback"
                }
                | _
              ]
            }} =
             SellApp.Feedback.v2_search_feedback(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "v2_search_feedback handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Feedback.v2_search_feedback(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_get_feedback succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/feedback/test_feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/v2_get_feedback"))
    end)

    assert {:ok, %SellApp.SdkV2GetFeedbackResponseValue200ApplicationJson{}} =
             SellApp.Feedback.v2_get_feedback(client, "test_feedback")
  end

  test "v2_replace_feedback succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/feedback/test_feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/v2_replace_feedback"))
    end)

    assert {:ok, %SellApp.SdkV2ReplaceFeedbackResponseValue200ApplicationJson{}} =
             SellApp.Feedback.v2_replace_feedback(client, "test_feedback", %{"reply" => "reply"})
  end

  test "v2_reply_to_feedback succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/feedback/test_feedback")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/v2_reply_to_feedback"))
    end)

    assert {:ok, %SellApp.SdkV2ReplyToFeedbackResponseValue200ApplicationJson{}} =
             SellApp.Feedback.v2_reply_to_feedback(client, "test_feedback", %{"reply" => "reply"})
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/list"))
    end)

    assert {:ok, _} = SellApp.Feedback.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("feedback/list"))
    end)

    SellApp.Feedback.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Feedback.list(client)
  end
end
