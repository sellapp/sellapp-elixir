# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.GroupsProductsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "add succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products/attach")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/add"))
    end)

    assert {:ok, %SellApp.SdkAddProductsToGroupResponseValue200ApplicationJson{}} =
             SellApp.GroupsProducts.add(client, "test_group", %{"resources" => [1]})
  end

  test "remove succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products/detach")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/remove"))
    end)

    assert {:ok, %SellApp.SdkRemoveProductsFromGroupResponseValue200ApplicationJson{}} =
             SellApp.GroupsProducts.remove(client, "test_group", %{"resources" => [1]})
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} = SellApp.GroupsProducts.list(client, "test_group")
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.GroupsProducts.list(client, "test_group")

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/replace"))
    end)

    assert {:ok, %SellApp.SdkSyncGroupProductsResponseValue200ApplicationJson{}} =
             SellApp.GroupsProducts.replace(client, "test_group", %{"product_ids" => [1]})
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products/test_product")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/get"))
    end)

    assert {:ok, %SellApp.SdkGetProductWithinGroupResponseValue200ApplicationJson{}} =
             SellApp.GroupsProducts.get(client, "test_group", "test_product")
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group/products/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} =
             SellApp.GroupsProducts.search(client, "test_group", %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.GroupsProducts.search(client, "test_group", %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/list"))
    end)

    assert {:ok, _} = SellApp.GroupsProducts.list(client, "test_group", %{limit: "hello world&x"})
  end

  test "add honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups_products/add"))
    end)

    SellApp.GroupsProducts.add(
      client,
      "test_group",
      %{"resources" => [1]},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.GroupsProducts.add(client, "test_group", %{"resources" => [1]})
  end
end
