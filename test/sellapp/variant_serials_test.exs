# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.VariantSerialsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/serials"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem{
                  id: "id",
                  serial: "serial"
                }
                | _
              ]
            }} = SellApp.VariantSerials.list(client, "test_product", "test_variant")
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.VariantSerials.list(client, "test_product", "test_variant")

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "append succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/serials"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/append"))
    end)

    assert {:ok, %SellApp.SdkAppendVariantSerialInventoryResponseValue201ApplicationJson{}} =
             SellApp.VariantSerials.append(client, "test_product", "test_variant", %{
               "serials" => ["serials"],
               "remove_duplicates" => true
             })
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/serials"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson{}} =
             SellApp.VariantSerials.replace(client, "test_product", "test_variant", %{
               "serials" => ["serials"],
               "remove_duplicates" => true
             })
  end

  test "queue succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/serials/import"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/queue"))
    end)

    assert {:ok, %SellApp.SdkQueueVariantSerialImportResponseValue202ApplicationJson{}} =
             SellApp.VariantSerials.queue(client, "test_product", "test_variant", %{
               "file" => "binary",
               "parsing_mode" => "COMMA",
               "custom_delimiter" => nil,
               "remove_duplicates" => true,
               "mode" => "append"
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/serials/test_serial"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteVariantSerialResponseValue200ApplicationJson{}} =
             SellApp.VariantSerials.delete(client, "test_product", "test_variant", "test_serial")
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["search"] == "hello world&x"
      refute Map.has_key?(params, "limit")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/list"))
    end)

    assert {:ok, _} =
             SellApp.VariantSerials.list(client, "test_product", "test_variant", %{
               search: "hello world&x"
             })
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_serials/list"))
    end)

    SellApp.VariantSerials.list(
      client,
      "test_product",
      "test_variant",
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.VariantSerials.list(client, "test_product", "test_variant")
  end
end
