# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.OrdersTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/orders")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListOrdersResponseValue200ApplicationJsonPropertyDataItem{id: 1} | _
              ]
            }} = SellApp.Orders.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Orders.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/create"))
    end)

    assert {:ok, %SellApp.SdkCreateOrderResponseValue201ApplicationJson{}} =
             SellApp.Orders.create(client, %{
               "customer_email" => "customer_email",
               "customer_ip" => nil,
               "payment_method" => "AUTHNET",
               "coupon" => "coupon",
               "vat_id" => "vat_id",
               "country" => "country",
               "affiliate" => nil,
               "extra" => %{"amount" => 1, "currency" => "currency"},
               "product_variants" => %{},
               "custom_payment_method_id" => nil
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchOrdersResponseValue200ApplicationJsonPropertyDataItem{id: 1} | _
              ]
            }} =
             SellApp.Orders.search(client, %{
               "filters" => [%{"field" => "field", "operator" => "eq", "value" => nil}],
               "sort" => [%{"field" => "created_at", "direction" => "asc"}],
               "pagination" => %{"page" => 1, "limit" => 1}
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Orders.search(client, %{
               "filters" => [%{"field" => "field", "operator" => "eq", "value" => nil}],
               "sort" => [%{"field" => "created_at", "direction" => "asc"}],
               "pagination" => %{"page" => 1, "limit" => 1}
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/get"))
    end)

    assert {:ok, %SellApp.SdkGetOrderResponseValue200ApplicationJson{}} =
             SellApp.Orders.get(client, "test_order")
  end

  test "update_status succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/status")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/update_status"))
    end)

    assert {:ok, %SellApp.SdkUpdateOrderStatusResponseValue200ApplicationJson{}} =
             SellApp.Orders.update_status(client, "test_order", %{
               "status" => "COMPLETED",
               "expected_status" => nil
             })
  end

  test "create_checkout succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/checkout")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/create_checkout"))
    end)

    assert {:ok,
            %SellApp.SdkCreateOrderCheckoutResponseValue200ApplicationJson{message: "message"}} =
             SellApp.Orders.create_checkout(client, "test_order", %{"expected_status" => nil})
  end

  test "create_replacement succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/replacements")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/create_replacement"))
    end)

    assert {:ok, %SellApp.SdkCreateOrderReplacementResponseValue200ApplicationJson{}} =
             SellApp.Orders.create_replacement(client, "test_order", %{"product_variants" => %{}})
  end

  test "create_refund succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/refunds")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/create_refund"))
    end)

    assert {:ok, %SellApp.SdkCreateOrderRefundResponseValue200ApplicationJson{}} =
             SellApp.Orders.create_refund(client, "test_order", %{"amount" => nil})
  end

  test "retry_fulfillment succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/fulfillment-retries")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/retry_fulfillment"))
    end)

    assert {:ok, %SellApp.SdkRetryOrderFulfillmentResponseValue200ApplicationJson{}} =
             SellApp.Orders.retry_fulfillment(client, "test_order", %{"email" => nil})
  end

  test "retry_dynamic_delivery succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/orders/test_order/dynamic-delivery-retries"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/retry_dynamic_delivery"))
    end)

    assert {:ok, %SellApp.SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson{}} =
             SellApp.Orders.retry_dynamic_delivery(client, "test_order", %{
               "delivered_product_id" => 1
             })
  end

  test "send_fulfillment_notifications succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/orders/test_order/fulfillment-notifications"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/send_fulfillment_notifications"))
    end)

    assert {:ok, %SellApp.SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson{}} =
             SellApp.Orders.send_fulfillment_notifications(client, "test_order", %{
               "email" => nil,
               "product_variant_ids" => nil
             })
  end

  test "list_deliverables succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/deliverables")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/list_deliverables"))
    end)

    assert {:ok, %SellApp.SdkListOrderDeliverablesResponseValue200ApplicationJson{}} =
             SellApp.Orders.list_deliverables(client, "test_order")
  end

  test "create_from_wallet succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/wallet")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/create_from_wallet"))
    end)

    assert {:ok, %SellApp.SdkCreateWalletOrderResponseValue201ApplicationJson{}} =
             SellApp.Orders.create_from_wallet(client, %{
               "customer_email" => "customer_email",
               "customer_ip" => nil,
               "coupon" => "coupon",
               "vat_id" => "vat_id",
               "country" => "country",
               "affiliate" => nil,
               "extra" => %{"amount" => 1, "currency" => "currency"},
               "product_variants" => %{}
             })
  end

  test "pay_from_wallet succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/orders/test_order/wallet-payments")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/pay_from_wallet"))
    end)

    assert {:ok, %SellApp.SdkPayOrderFromWalletResponseValue200ApplicationJson{}} =
             SellApp.Orders.pay_from_wallet(client, "test_order", %{"expected_status" => nil})
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/list"))
    end)

    assert {:ok, _} = SellApp.Orders.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("orders/list"))
    end)

    SellApp.Orders.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Orders.list(client)
  end
end
