# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.OAuthManagementTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "get_oauth_installation succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/oauth/installation")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("oauth_management/get_oauth_installation"))
    end)

    assert {:ok, %SellApp.SdkGetOAuthInstallationResponseValue200ApplicationJson{}} =
             SellApp.OAuthManagement.get_oauth_installation(client)
  end

  test "delete_oauth_installation succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/oauth/installation")
      Plug.Conn.send_resp(conn, 204, "")
    end)

    assert {:ok, _} = SellApp.OAuthManagement.delete_oauth_installation(client)
  end

  test "get_oauth_installation honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("oauth_management/get_oauth_installation"))
    end)

    SellApp.OAuthManagement.get_oauth_installation(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.OAuthManagement.get_oauth_installation(client)
  end
end
