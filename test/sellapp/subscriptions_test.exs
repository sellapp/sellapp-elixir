# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.SubscriptionsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "cancel succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/subscriptions/test_subscription/cancel")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/cancel"))
    end)

    assert {:ok,
            %SellApp.SdkCancelSubscriptionResponseValue200ApplicationJson{message: "message"}} =
             SellApp.Subscriptions.cancel(client, "test_subscription", %{
               "cancel_at_period_end" => true,
               "refund_last_payment" => true,
               "pro_rated_refund" => true,
               "idempotency_key" => nil
             })
  end

  test "get_capabilities succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/capabilities"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/get_capabilities"))
    end)

    assert {:ok, %SellApp.SdkGetSubscriptionCapabilitiesResponseValue200ApplicationJson{}} =
             SellApp.Subscriptions.get_capabilities(client, "test_product_subscription")
  end

  test "cancel_at_period_end succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/cancel-period-end"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/cancel_at_period_end"))
    end)

    assert {:ok,
            %SellApp.SdkCancelSubscriptionAtPeriodEndResponseValue200ApplicationJson{
              message: "message"
            }} =
             SellApp.Subscriptions.cancel_at_period_end(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "reason" => nil
             })
  end

  test "cancel_immediately succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/cancel-immediately"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/cancel_immediately"))
    end)

    assert {:ok,
            %SellApp.SdkCancelSubscriptionImmediatelyResponseValue200ApplicationJson{
              message: "message"
            }} =
             SellApp.Subscriptions.cancel_immediately(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "reason" => nil,
               "refund_last_payment" => true,
               "pro_rated_refund" => true
             })
  end

  test "pause succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/pause"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/pause"))
    end)

    assert {:ok, %SellApp.SdkPauseSubscriptionResponseValue200ApplicationJson{message: "message"}} =
             SellApp.Subscriptions.pause(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "resume_at" => nil,
               "reason" => nil
             })
  end

  test "resume succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/resume"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/resume"))
    end)

    assert {:ok,
            %SellApp.SdkResumeSubscriptionResponseValue200ApplicationJson{message: "message"}} =
             SellApp.Subscriptions.resume(client, "test_product_subscription", %{
               "idempotency_key" => nil
             })
  end

  test "update_payment_method succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/update-payment-method"
             )

      Plug.Conn.send_resp(conn, 204, "")
    end)

    assert {:ok, _} =
             SellApp.Subscriptions.update_payment_method(client, "test_product_subscription", %{
               "idempotency_key" => nil
             })
  end

  test "preview_plan_change succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/change-plan/preview"
             )

      Plug.Conn.send_resp(conn, 204, "")
    end)

    assert {:ok, _} =
             SellApp.Subscriptions.preview_plan_change(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "target_variant_id" => 1,
               "effective_timing" => "immediate",
               "proration_behavior" => "invoice_now",
               "metadata" => %{}
             })
  end

  test "confirm_plan_change succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/change-plan/confirm"
             )

      Plug.Conn.send_resp(conn, 204, "")
    end)

    assert {:ok, _} =
             SellApp.Subscriptions.confirm_plan_change(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "target_variant_id" => 1,
               "preview_token" => "preview_token",
               "effective_timing" => "immediate",
               "proration_behavior" => "invoice_now",
               "metadata" => %{}
             })
  end

  test "preview_renewal_date succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/change-renewal-date/preview"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/preview_renewal_date"))
    end)

    assert {:ok,
            %SellApp.SdkPreviewSubscriptionRenewalDateChangeResponseValue200ApplicationJson{
              message: "message"
            }} =
             SellApp.Subscriptions.preview_renewal_date(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "renewal_date" => "2024-01-01T00:00:00.000Z",
               "reason" => nil,
               "metadata" => %{}
             })
  end

  test "confirm_renewal_date succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/subscriptions/test_product_subscription/actions/change-renewal-date/confirm"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/confirm_renewal_date"))
    end)

    assert {:ok,
            %SellApp.SdkConfirmSubscriptionRenewalDateChangeResponseValue200ApplicationJson{
              message: "message"
            }} =
             SellApp.Subscriptions.confirm_renewal_date(client, "test_product_subscription", %{
               "idempotency_key" => nil,
               "renewal_date" => "2024-01-01T00:00:00.000Z",
               "preview_token" => nil,
               "reason" => nil,
               "metadata" => %{}
             })
  end

  test "list_subscriptions succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/subscriptions")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/list_subscriptions"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListSubscriptionsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  status: "status"
                }
                | _
              ]
            }} = SellApp.Subscriptions.list_subscriptions(client)
  end

  test "list_subscriptions handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Subscriptions.list_subscriptions(client)

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "search_subscriptions succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/subscriptions/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/search_subscriptions"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchSubscriptionsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  status: "status"
                }
                | _
              ]
            }} =
             SellApp.Subscriptions.search_subscriptions(client, %{
               "search" => "search",
               "status" => "status"
             })
  end

  test "search_subscriptions handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Subscriptions.search_subscriptions(client, %{
               "search" => "search",
               "status" => "status"
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get_subscription succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/subscriptions/test_product_subscription")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/get_subscription"))
    end)

    assert {:ok, %SellApp.SdkGetSubscriptionResponseValue200ApplicationJson{}} =
             SellApp.Subscriptions.get_subscription(client, "test_product_subscription")
  end

  test "cancel honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("subscriptions/cancel"))
    end)

    SellApp.Subscriptions.cancel(
      client,
      "test_subscription",
      %{
        "cancel_at_period_end" => true,
        "refund_last_payment" => true,
        "pro_rated_refund" => true,
        "idempotency_key" => nil
      },
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.Subscriptions.cancel(client, "test_subscription", %{
               "cancel_at_period_end" => true,
               "refund_last_payment" => true,
               "pro_rated_refund" => true,
               "idempotency_key" => nil
             })
  end
end
