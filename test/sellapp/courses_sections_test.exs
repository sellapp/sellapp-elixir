# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CoursesSectionsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/sections")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/create"))
    end)

    assert {:ok, %SellApp.SdkCreateCourseSectionResponseValue201ApplicationJson{}} =
             SellApp.CoursesSections.create(client, "test_course", %{
               "title" => "title",
               "description" => nil
             })
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/sections/test_section")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCourseSectionResponseValue200ApplicationJson{}} =
             SellApp.CoursesSections.replace(client, "test_course", "test_section", %{
               "title" => "title",
               "description" => nil,
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/sections/test_section")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCourseSectionResponseValue200ApplicationJson{}} =
             SellApp.CoursesSections.update(client, "test_course", "test_section", %{
               "title" => "title",
               "description" => nil,
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/sections/test_section")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteCourseSectionResponseValue200ApplicationJson{}} =
             SellApp.CoursesSections.delete(client, "test_course", "test_section")
  end

  test "reorder succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/sections/reorder")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/reorder"))
    end)

    assert {:ok, %SellApp.SdkReorderCourseSectionsResponseValue200ApplicationJson{}} =
             SellApp.CoursesSections.reorder(client, "test_course", %{"resources" => [1]})
  end

  test "create honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_sections/create"))
    end)

    SellApp.CoursesSections.create(
      client,
      "test_course",
      %{"title" => "title", "description" => nil},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.CoursesSections.create(client, "test_course", %{
               "title" => "title",
               "description" => nil
             })
  end
end
