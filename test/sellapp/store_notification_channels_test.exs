# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.StoreNotificationChannelsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/store/notification-channels")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/list"))
    end)

    assert {:ok, %SellApp.SdkListNotificationChannelsResponseValue200ApplicationJson{}} =
             SellApp.StoreNotificationChannels.list(client)
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/store/notification-channels")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/create"))
    end)

    assert {:ok, %SellApp.SdkCreateNotificationChannelResponseValue201ApplicationJson{}} =
             SellApp.StoreNotificationChannels.create(client, %{"channel" => %{}})
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/store/notification-channels/test_notification_channel"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/get"))
    end)

    assert {:ok, %SellApp.SdkGetNotificationChannelResponseValue200ApplicationJson{}} =
             SellApp.StoreNotificationChannels.get(client, "test_notification_channel")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/store/notification-channels/test_notification_channel"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceNotificationChannelResponseValue200ApplicationJson{}} =
             SellApp.StoreNotificationChannels.replace(client, "test_notification_channel", %{
               "channel" => %{}
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"

      assert String.ends_with?(
               conn.request_path,
               "/v2/store/notification-channels/test_notification_channel"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateNotificationChannelResponseValue200ApplicationJson{}} =
             SellApp.StoreNotificationChannels.update(client, "test_notification_channel", %{
               "channel" => %{
                 "type" => "email",
                 "email" => "email",
                 "channel" => "channel",
                 "allowed_notifications" => ["allowed_notifications"]
               }
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"

      assert String.ends_with?(
               conn.request_path,
               "/v2/store/notification-channels/test_notification_channel"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteNotificationChannelResponseValue200ApplicationJson{}} =
             SellApp.StoreNotificationChannels.delete(client, "test_notification_channel")
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("store_notification_channels/list"))
    end)

    SellApp.StoreNotificationChannels.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.StoreNotificationChannels.list(client)
  end
end
