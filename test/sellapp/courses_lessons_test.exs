# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CoursesLessonsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/courses/test_course/sections/test_section/lessons"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/create"))
    end)

    assert {:ok, %SellApp.SdkCreateCourseLessonResponseValue201ApplicationJson{}} =
             SellApp.CoursesLessons.create(client, "test_course", "test_section", %{
               "title" => "title",
               "type" => "lecture",
               "content" => nil,
               "is_preview" => true,
               "is_published" => true,
               "assignment" => %{
                 "estimated_duration_minutes" => nil,
                 "instructions" => nil,
                 "questions" => [%{"prompt" => "prompt", "solution" => nil}]
               },
               "questions" => [
                 %{
                   "prompt" => "prompt",
                   "question_type" => "text",
                   "solution" => nil,
                   "answers" => [%{"answer" => "answer", "is_correct" => true}]
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/lessons/test_lesson")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCourseLessonResponseValue200ApplicationJson{}} =
             SellApp.CoursesLessons.replace(client, "test_course", "test_lesson", %{
               "title" => "title",
               "type" => "lecture",
               "content" => nil,
               "is_preview" => true,
               "is_published" => true,
               "assignment" => %{
                 "estimated_duration_minutes" => nil,
                 "instructions" => nil,
                 "questions" => [%{"prompt" => "prompt", "solution" => nil}]
               },
               "questions" => [
                 %{
                   "prompt" => "prompt",
                   "question_type" => "text",
                   "solution" => nil,
                   "answers" => [%{"answer" => "answer", "is_correct" => true}]
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/lessons/test_lesson")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCourseLessonResponseValue200ApplicationJson{}} =
             SellApp.CoursesLessons.update(client, "test_course", "test_lesson", %{
               "title" => "title",
               "type" => "lecture",
               "content" => nil,
               "is_preview" => true,
               "is_published" => true,
               "assignment" => %{
                 "estimated_duration_minutes" => nil,
                 "instructions" => nil,
                 "questions" => [%{"prompt" => "prompt", "solution" => nil}]
               },
               "questions" => [
                 %{
                   "prompt" => "prompt",
                   "question_type" => "text",
                   "solution" => nil,
                   "answers" => [%{"answer" => "answer", "is_correct" => true}]
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/lessons/test_lesson")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteCourseLessonResponseValue200ApplicationJson{}} =
             SellApp.CoursesLessons.delete(client, "test_course", "test_lesson")
  end

  test "reorder succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course/lessons/reorder")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/reorder"))
    end)

    assert {:ok, %SellApp.SdkReorderCourseLessonsResponseValue200ApplicationJson{}} =
             SellApp.CoursesLessons.reorder(client, "test_course", %{
               "resources" => [%{"id" => 1, "section_id" => 1}]
             })
  end

  test "create honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses_lessons/create"))
    end)

    SellApp.CoursesLessons.create(
      client,
      "test_course",
      "test_section",
      %{
        "title" => "title",
        "type" => "lecture",
        "content" => nil,
        "is_preview" => true,
        "is_published" => true,
        "assignment" => %{
          "estimated_duration_minutes" => nil,
          "instructions" => nil,
          "questions" => [%{"prompt" => "prompt", "solution" => nil}]
        },
        "questions" => [
          %{
            "prompt" => "prompt",
            "question_type" => "text",
            "solution" => nil,
            "answers" => [%{"answer" => "answer", "is_correct" => true}]
          }
        ],
        "expected_updated_at" => "2024-01-01T00:00:00.000Z"
      },
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.CoursesLessons.create(client, "test_course", "test_section", %{
               "title" => "title",
               "type" => "lecture",
               "content" => nil,
               "is_preview" => true,
               "is_published" => true,
               "assignment" => %{
                 "estimated_duration_minutes" => nil,
                 "instructions" => nil,
                 "questions" => [%{"prompt" => "prompt", "solution" => nil}]
               },
               "questions" => [
                 %{
                   "prompt" => "prompt",
                   "question_type" => "text",
                   "solution" => nil,
                   "answers" => [%{"answer" => "answer", "is_correct" => true}]
                 }
               ],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end
end
