# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PaymentMethodsCustomTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/payment-methods/custom")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/list"))
    end)

    assert {:ok, %SellApp.SdkListCustomPaymentMethodsResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.list(client)
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/payment-methods/custom")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/create"))
    end)

    assert {:ok, %SellApp.SdkCreateCustomPaymentMethodResponseValue201ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.create(client, %{
               "type" => "instructions",
               "name" => "name",
               "description" => nil,
               "instructions" => nil,
               "steps" => nil,
               "redirect_url" => nil,
               "skip_interstitial_page" => true,
               "show_processing_status_page" => true,
               "require_proof_of_payment" => true,
               "enabled" => true,
               "sort_order" => 1,
               "modifier" => nil
             })
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/custom/test_custom_payment_method"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/get"))
    end)

    assert {:ok, %SellApp.SdkGetCustomPaymentMethodResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.get(client, "test_custom_payment_method")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/custom/test_custom_payment_method"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.replace(client, "test_custom_payment_method", %{
               "type" => "instructions",
               "name" => "name",
               "description" => nil,
               "instructions" => nil,
               "steps" => nil,
               "redirect_url" => nil,
               "skip_interstitial_page" => true,
               "show_processing_status_page" => true,
               "require_proof_of_payment" => true,
               "enabled" => true,
               "sort_order" => 1,
               "modifier" => nil
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/custom/test_custom_payment_method"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.update(client, "test_custom_payment_method", %{
               "type" => "instructions",
               "name" => "name",
               "description" => nil,
               "instructions" => nil,
               "steps" => nil,
               "redirect_url" => nil,
               "skip_interstitial_page" => true,
               "show_processing_status_page" => true,
               "require_proof_of_payment" => true,
               "enabled" => true,
               "sort_order" => 1,
               "modifier" => nil
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/custom/test_custom_payment_method"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteCustomPaymentMethodResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethodsCustom.delete(client, "test_custom_payment_method")
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods_custom/list"))
    end)

    SellApp.PaymentMethodsCustom.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.PaymentMethodsCustom.list(client)
  end
end
