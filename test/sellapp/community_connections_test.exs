# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CommunityConnectionsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/community-connections")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/list"))
    end)

    assert {:ok, %SellApp.SdkListCommunityConnectionsResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.list(client)
  end

  test "start succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/community-connections/test_platform/connect"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/start"))
    end)

    assert {:ok, %SellApp.SdkStartCommunityConnectionResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.start(client, "test_platform", %{"mode" => "account"})
  end

  test "poll succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/community-connections/test_platform/status"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/poll"))
    end)

    assert {:ok, %SellApp.SdkPollCommunityConnectionResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.poll(client, "test_platform")
  end

  test "complete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/community-connections/test_platform/complete"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/complete"))
    end)

    assert {:ok, %SellApp.SdkCompleteCommunityConnectionResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.complete(client, "test_platform", %{
               "status_token" => "status_token",
               "server_id" => "server_id"
             })
  end

  test "verify succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/community-connections/test_platform/verify"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/verify"))
    end)

    assert {:ok, %SellApp.SdkVerifyCommunityConnectionResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.verify(client, "test_platform")
  end

  test "disconnect succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/community-connections/test_platform")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/disconnect"))
    end)

    assert {:ok, %SellApp.SdkDisconnectCommunityPlatformResponseValue200ApplicationJson{}} =
             SellApp.CommunityConnections.disconnect(client, "test_platform")
  end

  test "poll encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["status_token"] == "hello world&x"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/poll"))
    end)

    assert {:ok, _} =
             SellApp.CommunityConnections.poll(client, "test_platform", %{
               status_token: "hello world&x"
             })
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("community_connections/list"))
    end)

    SellApp.CommunityConnections.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.CommunityConnections.list(client)
  end
end
