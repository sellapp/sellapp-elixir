# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.GroupsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/groups")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListGroupsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} = SellApp.Groups.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Groups.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/groups")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/create"))
    end)

    assert {:ok, %SellApp.SdkCreateGroupResponseValue201ApplicationJson{}} =
             SellApp.Groups.create(client, %{
               "title" => "title",
               "unlisted" => true,
               "order" => nil,
               "product_ids" => [1],
               "section_id" => nil
             })
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/get"))
    end)

    assert {:ok, %SellApp.SdkGetGroupResponseValue200ApplicationJson{}} =
             SellApp.Groups.get(client, "test_group")
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateGroupResponseValue200ApplicationJson{}} =
             SellApp.Groups.update(client, "test_group", %{
               "title" => "title",
               "unlisted" => true,
               "order" => nil,
               "product_ids" => [1],
               "section_id" => nil
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/groups/test_group")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteGroupResponseValue200ApplicationJson{}} =
             SellApp.Groups.delete(client, "test_group")
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/groups/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchGroupsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} =
             SellApp.Groups.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Groups.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/list"))
    end)

    assert {:ok, _} = SellApp.Groups.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("groups/list"))
    end)

    SellApp.Groups.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Groups.list(client)
  end
end
