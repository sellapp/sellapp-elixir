# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CashbackRulesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/list"))
    end)

    assert {:ok, %SellApp.SdkListCashbackRulesResponseValue200ApplicationJson{}} =
             SellApp.CashbackRules.list(client)
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/create"))
    end)

    assert {:ok, %SellApp.SdkCreateCashbackRuleResponseValue201ApplicationJson{}} =
             SellApp.CashbackRules.create(client, %{
               "percent_basis" => 1,
               "maximum_cashback_cents" => nil,
               "is_active" => true,
               "product_ids" => [1]
             })
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules/test_cashback_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCashbackRuleResponseValue200ApplicationJson{}} =
             SellApp.CashbackRules.replace(client, "test_cashback_rule", %{
               "percent_basis" => 1,
               "maximum_cashback_cents" => nil,
               "is_active" => true,
               "product_ids" => [1]
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules/test_cashback_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCashbackRuleResponseValue200ApplicationJson{}} =
             SellApp.CashbackRules.update(client, "test_cashback_rule", %{
               "percent_basis" => 1,
               "maximum_cashback_cents" => nil,
               "is_active" => true,
               "product_ids" => [1]
             })
  end

  test "archive succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules/test_cashback_rule")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/archive"))
    end)

    assert {:ok, %SellApp.SdkArchiveCashbackRuleResponseValue200ApplicationJson{}} =
             SellApp.CashbackRules.archive(client, "test_cashback_rule")
  end

  test "restore succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/cashback-rules/test_cashback_rule/restore")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/restore"))
    end)

    assert {:ok, %SellApp.SdkRestoreCashbackRuleResponseValue200ApplicationJson{}} =
             SellApp.CashbackRules.restore(client, "test_cashback_rule")
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("cashback_rules/list"))
    end)

    SellApp.CashbackRules.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.CashbackRules.list(client)
  end
end
