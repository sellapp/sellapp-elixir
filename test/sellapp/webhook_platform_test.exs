# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WebhookPlatformTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list_webhook_event_types succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/webhook-event-types")

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("webhook_platform/list_webhook_event_types")
      )
    end)

    assert {:ok, %SellApp.SdkListWebhookEventTypesResponseValue200ApplicationJson{}} =
             SellApp.WebhookPlatform.list_webhook_event_types(client)
  end

  test "list_webhook_deliveries succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/webhook-deliveries")

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("webhook_platform/list_webhook_deliveries")
      )
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem{
                  id: "00000000-0000-4000-8000-000000000000",
                  event_id: "evt_00000000000000000000000000"
                }
                | _
              ]
            }} = SellApp.WebhookPlatform.list_webhook_deliveries(client)
  end

  test "list_webhook_deliveries handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.WebhookPlatform.list_webhook_deliveries(client)

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get_webhook_delivery succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/webhook-deliveries/test_delivery")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_platform/get_webhook_delivery"))
    end)

    assert {:ok, %SellApp.SdkGetWebhookDeliveryResponseValue200ApplicationJson{}} =
             SellApp.WebhookPlatform.get_webhook_delivery(client, "test_delivery")
  end

  test "replay_webhook_delivery succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/webhook-deliveries/test_delivery/replay")

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("webhook_platform/replay_webhook_delivery")
      )
    end)

    assert {:ok, %SellApp.SdkReplayWebhookDeliveryResponseValue201ApplicationJson{}} =
             SellApp.WebhookPlatform.replay_webhook_delivery(client, "test_delivery", %{})
  end

  test "list_webhook_event_types honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"

      Req.Test.json(
        conn,
        SellApp.TestFixtures.fixture("webhook_platform/list_webhook_event_types")
      )
    end)

    SellApp.WebhookPlatform.list_webhook_event_types(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.WebhookPlatform.list_webhook_event_types(client)
  end
end
