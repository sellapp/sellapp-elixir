# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.RewardCouponTemplatesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/reward-coupon-templates")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkListRewardCouponTemplatesResponseValue200ApplicationJson{} | _]
            }} = SellApp.RewardCouponTemplates.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.RewardCouponTemplates.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/reward-coupon-templates")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/create"))
    end)

    assert {:ok, %SellApp.SdkCreateRewardCouponTemplateResponseValue201ApplicationJson{}} =
             SellApp.RewardCouponTemplates.create(client, %{
               "name" => "name",
               "type" => "PERCENTAGE",
               "discount" => "discount",
               "store_wide" => true,
               "minimum_amount" => nil,
               "maximum_discount_amount" => nil,
               "expires_at" => nil,
               "expires_after_days" => nil,
               "redemption_mode" => "customer_locked",
               "is_active" => true,
               "listing_ids" => [1],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/reward-coupon-templates/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkSearchRewardCouponTemplatesResponseValue200ApplicationJson{} | _]
            }} =
             SellApp.RewardCouponTemplates.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.RewardCouponTemplates.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/reward-coupon-templates/test_reward_coupon_template"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/get"))
    end)

    assert {:ok, %SellApp.SdkGetRewardCouponTemplateResponseValue200ApplicationJson{}} =
             SellApp.RewardCouponTemplates.get(client, "test_reward_coupon_template")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/reward-coupon-templates/test_reward_coupon_template"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceRewardCouponTemplateResponseValue200ApplicationJson{}} =
             SellApp.RewardCouponTemplates.replace(client, "test_reward_coupon_template", %{
               "name" => "name",
               "type" => "PERCENTAGE",
               "discount" => "discount",
               "store_wide" => true,
               "minimum_amount" => nil,
               "maximum_discount_amount" => nil,
               "expires_at" => nil,
               "expires_after_days" => nil,
               "redemption_mode" => "customer_locked",
               "is_active" => true,
               "listing_ids" => [1],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"

      assert String.ends_with?(
               conn.request_path,
               "/v2/reward-coupon-templates/test_reward_coupon_template"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateRewardCouponTemplateResponseValue200ApplicationJson{}} =
             SellApp.RewardCouponTemplates.update(client, "test_reward_coupon_template", %{
               "name" => "name",
               "type" => "PERCENTAGE",
               "discount" => "discount",
               "store_wide" => true,
               "minimum_amount" => nil,
               "maximum_discount_amount" => nil,
               "expires_at" => nil,
               "expires_after_days" => nil,
               "redemption_mode" => "customer_locked",
               "is_active" => true,
               "listing_ids" => [1],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/list"))
    end)

    assert {:ok, _} = SellApp.RewardCouponTemplates.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("reward_coupon_templates/list"))
    end)

    SellApp.RewardCouponTemplates.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.RewardCouponTemplates.list(client)
  end
end
