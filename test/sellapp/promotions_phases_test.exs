# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PromotionsPhasesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion/phases")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions_phases/list"))
    end)

    assert {:ok, %SellApp.SdkListPromotionPhasesResponseValue200ApplicationJson{}} =
             SellApp.PromotionsPhases.list(client, "test_promotion")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion/phases")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions_phases/replace"))
    end)

    assert {:ok, %SellApp.SdkReplacePromotionPhasesResponseValue200ApplicationJson{}} =
             SellApp.PromotionsPhases.replace(client, "test_promotion", %{
               "phases" => [
                 %{
                   "discount_type" => "percentage",
                   "discount_value" => 1,
                   "ends_at" => nil,
                   "max_redemptions" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions_phases/list"))
    end)

    SellApp.PromotionsPhases.list(
      client,
      "test_promotion",
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.PromotionsPhases.list(client, "test_promotion")
  end
end
