# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CoursesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/courses")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/list"))
    end)

    assert {:ok,
            %SellApp.Page{data: [%SellApp.SdkListCoursesResponseValue200ApplicationJson{} | _]}} =
             SellApp.Courses.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Courses.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/courses/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/search"))
    end)

    assert {:ok,
            %SellApp.Page{data: [%SellApp.SdkSearchCoursesResponseValue200ApplicationJson{} | _]}} =
             SellApp.Courses.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Courses.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/get"))
    end)

    assert {:ok, %SellApp.SdkGetCourseResponseValue200ApplicationJson{}} =
             SellApp.Courses.get(client, "test_course")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceCourseResponseValue200ApplicationJson{}} =
             SellApp.Courses.replace(client, "test_course", %{
               "category" => nil,
               "level" => "all_levels",
               "language" => "language",
               "subtitle" => nil,
               "author" => nil,
               "subcategory" => nil,
               "what_you_learn" => ["what_you_learn"],
               "requirements" => ["requirements"],
               "certificate_enabled" => true,
               "access_type" => "lifetime",
               "access_duration_days" => nil,
               "enrollment_limit" => nil,
               "delivery_text" => nil,
               "visibility" => "PUBLIC",
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/courses/test_course")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateCourseResponseValue200ApplicationJson{}} =
             SellApp.Courses.update(client, "test_course", %{
               "category" => nil,
               "level" => "all_levels",
               "language" => "language",
               "subtitle" => nil,
               "author" => nil,
               "subcategory" => nil,
               "what_you_learn" => ["what_you_learn"],
               "requirements" => ["requirements"],
               "certificate_enabled" => true,
               "access_type" => "lifetime",
               "access_duration_days" => nil,
               "enrollment_limit" => nil,
               "delivery_text" => nil,
               "visibility" => "PUBLIC",
               "expected_updated_at" => "2024-01-01T00:00:00.000Z"
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/list"))
    end)

    assert {:ok, _} = SellApp.Courses.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("courses/list"))
    end)

    SellApp.Courses.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Courses.list(client)
  end
end
