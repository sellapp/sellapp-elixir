# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WebhookChannelsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem{
                  id: "00000000-0000-4000-8000-000000000000",
                  url: "url"
                }
                | _
              ]
            }} = SellApp.WebhookChannels.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.WebhookChannels.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/create"))
    end)

    assert {:ok, %SellApp.SdkCreateWebhookChannelResponseValue201ApplicationJson{}} =
             SellApp.WebhookChannels.create(client, %{
               "name" => nil,
               "url" => "url",
               "allowed_notifications" => ["booking.cancelled"]
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem{
                  id: "00000000-0000-4000-8000-000000000000",
                  url: "url"
                }
                | _
              ]
            }} =
             SellApp.WebhookChannels.search(client, %{
               "search" => %{"value" => nil},
               "event" => nil,
               "page" => 1,
               "limit" => 1
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.WebhookChannels.search(client, %{
               "search" => %{"value" => nil},
               "event" => nil,
               "page" => 1,
               "limit" => 1
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "rotate succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/signing-secret")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/rotate"))
    end)

    assert {:ok, %SellApp.SdkRotateWebhookSigningSecretResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.rotate(client, %{
               "signing_secret" => "signing_secretxxxxxxxxxxxxxxxxxx"
             })
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/test_webhook_channel")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/get"))
    end)

    assert {:ok, %SellApp.SdkGetWebhookChannelResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.get(client, "test_webhook_channel")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/test_webhook_channel")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceWebhookChannelResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.replace(client, "test_webhook_channel", %{
               "name" => nil,
               "url" => "url",
               "allowed_notifications" => ["booking.cancelled"]
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/test_webhook_channel")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateWebhookChannelResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.update(client, "test_webhook_channel", %{
               "name" => nil,
               "url" => "url",
               "allowed_notifications" => ["booking.cancelled"]
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/webhook-channels/test_webhook_channel")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/delete"))
    end)

    assert {:ok, %SellApp.SdkDeleteWebhookChannelResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.delete(client, "test_webhook_channel")
  end

  test "send succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/webhook-channels/test_webhook_channel/test"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/send"))
    end)

    assert {:ok, %SellApp.SdkSendTestWebhookResponseValue200ApplicationJson{}} =
             SellApp.WebhookChannels.send(client, "test_webhook_channel", %{
               "event" => "booking.cancelled"
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["page"] == "hello world&x"
      refute Map.has_key?(params, "limit")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/list"))
    end)

    assert {:ok, _} = SellApp.WebhookChannels.list(client, %{page: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("webhook_channels/list"))
    end)

    SellApp.WebhookChannels.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.WebhookChannels.list(client)
  end
end
