# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WalletSettingsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/wallet/settings")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_settings/get"))
    end)

    assert {:ok, %SellApp.SdkGetWalletSettingsResponseValue200ApplicationJson{}} =
             SellApp.WalletSettings.get(client)
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/wallet/settings")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_settings/replace"))
    end)

    assert {:ok, %SellApp.SdkReplaceWalletSettingsResponseValue200ApplicationJson{}} =
             SellApp.WalletSettings.replace(client, %{
               "enabled" => true,
               "minimum_top_up_cents" => nil,
               "maximum_top_up_cents" => nil,
               "expiration_days" => nil,
               "payment_methods" => ["payment_methods"]
             })
  end

  test "get honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_settings/get"))
    end)

    SellApp.WalletSettings.get(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.WalletSettings.get(client)
  end
end
