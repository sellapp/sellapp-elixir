# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.TicketsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/tickets")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/list"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.ListTicketsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} = SellApp.Tickets.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Tickets.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v1/tickets/test_ticket")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/get"))
    end)

    assert {:ok, %SellApp.SdkGetTicketResponseValue200ApplicationJson{}} =
             SellApp.Tickets.get(client, "test_ticket")
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v1/tickets/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.SearchTicketsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} =
             SellApp.Tickets.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Tickets.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_list_tickets succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/tickets")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/v2_list_tickets"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2ListTicketsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} = SellApp.Tickets.v2_list_tickets(client)
  end

  test "v2_list_tickets handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Tickets.v2_list_tickets(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_search_tickets succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/tickets/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/v2_search_tickets"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [
                %SellApp.V2SearchTicketsResponseValue200ApplicationJsonPropertyDataItem{
                  id: 1,
                  title: "title"
                }
                | _
              ]
            }} =
             SellApp.Tickets.v2_search_tickets(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "v2_search_tickets handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Tickets.v2_search_tickets(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "v2_get_ticket succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/tickets/test_ticket")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/v2_get_ticket"))
    end)

    assert {:ok, %SellApp.SdkV2GetTicketResponseValue200ApplicationJson{}} =
             SellApp.Tickets.v2_get_ticket(client, "test_ticket")
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/tickets/test_ticket")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/update"))
    end)

    assert {:ok, %SellApp.SdkUpdateTicketResponseValue200ApplicationJson{}} =
             SellApp.Tickets.update(client, "test_ticket", %{
               "status" => "OPEN",
               "archived" => true
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/list"))
    end)

    assert {:ok, _} = SellApp.Tickets.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("tickets/list"))
    end)

    SellApp.Tickets.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Tickets.list(client)
  end
end
