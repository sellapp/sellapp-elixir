# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WalletTopupsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/wallets/test_customer/top-ups")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_topups/create"))
    end)

    assert {:ok, %SellApp.SdkCreateWalletTopUpResponseValue201ApplicationJson{}} =
             SellApp.WalletTopups.create(client, "test_customer", %{
               "amount_cents" => 1,
               "payment_method" => "AUTHNET",
               "custom_payment_method_id" => nil
             })
  end

  test "create honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("wallet_topups/create"))
    end)

    SellApp.WalletTopups.create(
      client,
      "test_customer",
      %{"amount_cents" => 1, "payment_method" => "AUTHNET", "custom_payment_method_id" => nil},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.WalletTopups.create(client, "test_customer", %{
               "amount_cents" => 1,
               "payment_method" => "AUTHNET",
               "custom_payment_method_id" => nil
             })
  end
end
