# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PaymentMethodsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/payment-methods")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/list"))
    end)

    assert {:ok, %SellApp.SdkListPaymentMethodsResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethods.list(client)
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/payment-methods/test_payment_method")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/get"))
    end)

    assert {:ok, %SellApp.SdkGetPaymentMethodStatusResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethods.get(client, "test_payment_method")
  end

  test "enable succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/payment-methods/test_payment_method")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/enable"))
    end)

    assert {:ok, %SellApp.SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethods.enable(client, "test_payment_method", %{"enabled" => true})
  end

  test "connect succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/test_payment_method/connect"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/connect"))
    end)

    assert {:ok, %SellApp.SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethods.connect(client, "test_payment_method")
  end

  test "validate succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/payment-methods/test_payment_method/configuration"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/validate"))
    end)

    assert {:ok,
            %SellApp.SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson{}} =
             SellApp.PaymentMethods.validate(client, "test_payment_method", %{
               "merchant_secure_key" => "merchant_secure_key",
               "merchant_tokenization_key" => "merchant_tokenization_key",
               "signing_key" => "signing_key",
               "currencies" => ["currencies"]
             })
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("payment_methods/list"))
    end)

    SellApp.PaymentMethods.list(
      client,
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.PaymentMethods.list(client)
  end
end
