# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PromotionsTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "list succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/promotions")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/list"))
    end)

    assert {:ok,
            %SellApp.Page{data: [%SellApp.SdkListPromotionsResponseValue200ApplicationJson{} | _]}} =
             SellApp.Promotions.list(client)
  end

  test "list handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} = SellApp.Promotions.list(client)
    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "create succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/promotions")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/create"))
    end)

    assert {:ok, %SellApp.SdkCreatePromotionResponseValue201ApplicationJson{}} =
             SellApp.Promotions.create(client, %{
               "name" => "name",
               "status" => "active",
               "starts_at" => nil,
               "ends_at" => nil,
               "priority" => 1,
               "is_stackable" => true,
               "max_redemptions" => nil,
               "phases" => [
                 %{
                   "discount_type" => "percentage",
                   "discount_value" => 1,
                   "ends_at" => nil,
                   "max_redemptions" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "search succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/promotions/search")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/search"))
    end)

    assert {:ok,
            %SellApp.Page{
              data: [%SellApp.SdkSearchPromotionsResponseValue200ApplicationJson{} | _]
            }} =
             SellApp.Promotions.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })
  end

  test "search handles an empty page", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      Req.Test.json(conn, %{"data" => [], "list_metadata" => %{"after" => nil}})
    end)

    assert {:ok, %SellApp.Page{data: []} = page} =
             SellApp.Promotions.search(client, %{
               "filters" => [
                 %{
                   "field" => "field",
                   "operator" => "operator",
                   "value" => nil,
                   "type" => "and",
                   "nested" => [%{}]
                 }
               ],
               "sort" => [%{"field" => "field", "direction" => "asc"}],
               "search" => %{"value" => nil, "case_sensitive" => true},
               "includes" => [%{"relation" => "relation"}]
             })

    refute SellApp.Page.has_more?(page)
    assert page |> SellApp.Page.stream() |> Enum.to_list() == []
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/get"))
    end)

    assert {:ok, %SellApp.SdkGetPromotionResponseValue200ApplicationJson{}} =
             SellApp.Promotions.get(client, "test_promotion")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/replace"))
    end)

    assert {:ok, %SellApp.SdkReplacePromotionResponseValue200ApplicationJson{}} =
             SellApp.Promotions.replace(client, "test_promotion", %{
               "name" => "name",
               "status" => "active",
               "starts_at" => nil,
               "ends_at" => nil,
               "priority" => 1,
               "is_stackable" => true,
               "max_redemptions" => nil,
               "phases" => [
                 %{
                   "discount_type" => "percentage",
                   "discount_value" => 1,
                   "ends_at" => nil,
                   "max_redemptions" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "update succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PATCH"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/update"))
    end)

    assert {:ok, %SellApp.SdkUpdatePromotionResponseValue200ApplicationJson{}} =
             SellApp.Promotions.update(client, "test_promotion", %{
               "name" => "name",
               "status" => "active",
               "starts_at" => nil,
               "ends_at" => nil,
               "priority" => 1,
               "is_stackable" => true,
               "max_redemptions" => nil,
               "phases" => [
                 %{
                   "discount_type" => "percentage",
                   "discount_value" => 1,
                   "ends_at" => nil,
                   "max_redemptions" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "delete succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "DELETE"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/delete"))
    end)

    assert {:ok, %SellApp.SdkDeletePromotionResponseValue200ApplicationJson{}} =
             SellApp.Promotions.delete(client, "test_promotion")
  end

  test "restore succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "POST"
      assert String.ends_with?(conn.request_path, "/v2/promotions/test_promotion/restore")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/restore"))
    end)

    assert {:ok, %SellApp.SdkRestorePromotionResponseValue200ApplicationJson{}} =
             SellApp.Promotions.restore(client, "test_promotion", %{
               "name" => "name",
               "status" => "active",
               "starts_at" => nil,
               "ends_at" => nil,
               "priority" => 1,
               "is_stackable" => true,
               "max_redemptions" => nil,
               "phases" => [
                 %{
                   "discount_type" => "percentage",
                   "discount_value" => 1,
                   "ends_at" => nil,
                   "max_redemptions" => nil,
                   "minimum_amount" => nil
                 }
               ]
             })
  end

  test "list encodes query parameters", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      params = Plug.Conn.fetch_query_params(conn).query_params
      assert params["limit"] == "hello world&x"
      refute Map.has_key?(params, "page")
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/list"))
    end)

    assert {:ok, _} = SellApp.Promotions.list(client, %{limit: "hello world&x"})
  end

  test "list honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("promotions/list"))
    end)

    SellApp.Promotions.list(
      client,
      %{},
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} = SellApp.Promotions.list(client)
  end
end
