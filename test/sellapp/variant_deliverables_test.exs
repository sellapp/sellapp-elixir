# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.VariantDeliverablesTest do
  use ExUnit.Case, async: true

  setup do
    client =
      SellApp.Client.new(
        api_key: "sk_test_key",
        access_token: "oauth_test",
        customer_session: "customer_test",
        browser_session: "sell_session=test",
        store: "test-store",
        req_options: [plug: {Req.Test, SellApp.Client}]
      )

    {:ok, client: client}
  end

  test "get succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "GET"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/deliverable"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_deliverables/get"))
    end)

    assert {:ok, %SellApp.SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson{}} =
             SellApp.VariantDeliverables.get(client, "test_product", "test_variant")
  end

  test "replace succeeds", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert conn.method == "PUT"

      assert String.ends_with?(
               conn.request_path,
               "/v2/products/test_product/variants/test_variant/deliverable"
             )

      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_deliverables/replace"))
    end)

    assert {:ok,
            %SellApp.SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson{}} =
             SellApp.VariantDeliverables.replace(client, "test_product", "test_variant", %{
               "types" => ["DOWNLOADABLE"],
               "expected_updated_at" => "2024-01-01T00:00:00.000Z",
               "data" => %{
                 "comment" => nil,
                 "licenseLength" => nil,
                 "licenseLengthUnit" => nil,
                 "isLicenseLengthUnlimited" => true,
                 "licenseLimit" => nil,
                 "isLicenseLimitUnlimited" => true,
                 "licensePrefix" => nil
               }
             })
  end

  test "get honors per-request options", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      assert Plug.Conn.get_req_header(conn, "x-custom") == ["value"]
      assert conn.host == "override.example.com"
      Req.Test.json(conn, SellApp.TestFixtures.fixture("variant_deliverables/get"))
    end)

    SellApp.VariantDeliverables.get(
      client,
      "test_product",
      "test_variant",
      headers: [{"x-custom", "value"}],
      base_url: "https://override.example.com"
    )
  end

  test "surfaces API errors as tagged tuples", %{client: client} do
    Req.Test.stub(SellApp.Client, fn conn ->
      conn
      |> Plug.Conn.put_status(401)
      |> Req.Test.json(%{"message" => "Unauthorized"})
    end)

    assert {:error, %SellApp.Error.Authentication{status: 401}} =
             SellApp.VariantDeliverables.get(client, "test_product", "test_variant")
  end
end
