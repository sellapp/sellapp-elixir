# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PaymentMethodsCustom do
  @moduledoc """
  Operations for the PaymentMethodsCustom API.
  """

  @doc """
  List custom payment methods

  List the current store's instruction and redirect payment methods. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkListCustomPaymentMethodsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/payment-methods/custom",
             %{},
             Keyword.put(opts, :operation_id, "listCustomPaymentMethods")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkListCustomPaymentMethodsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListCustomPaymentMethodsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a custom payment method

  Create a store-scoped custom payment method. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateCustomPaymentMethodResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCreateCustomPaymentMethodRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/payment-methods/custom",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "createCustomPaymentMethod"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkCreateCustomPaymentMethodResponseValue201ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateCustomPaymentMethodResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a custom payment method

  Retrieve one custom payment method owned by the current store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_payment_method` — Store-scoped custom payment method ULID.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetCustomPaymentMethodResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, custom_payment_method, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/payment-methods/custom/#{URI.encode(custom_payment_method, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getCustomPaymentMethod")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetCustomPaymentMethodResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetCustomPaymentMethodResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace a custom payment method

  Update a custom payment method with required type and name. Omitted optional fields retain their stored values; this PUT does not reset every omitted field. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_payment_method` — Store-scoped custom payment method ULID.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, custom_payment_method, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceCustomPaymentMethodRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/payment-methods/custom/#{URI.encode(custom_payment_method, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceCustomPaymentMethod"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a custom payment method

  Partially update a store-scoped custom payment method; omitted fields retain their values. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_payment_method` — Store-scoped custom payment method ULID.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, custom_payment_method, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateCustomPaymentMethodRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/payment-methods/custom/#{URI.encode(custom_payment_method, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateCustomPaymentMethod"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a custom payment method

  Delete one store-scoped custom payment method and remove its product assignments. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_payment_method` — Store-scoped custom payment method ULID.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDeleteCustomPaymentMethodResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def delete(client, custom_payment_method, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/payment-methods/custom/#{URI.encode(custom_payment_method, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteCustomPaymentMethod")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkDeleteCustomPaymentMethodResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDeleteCustomPaymentMethodResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
