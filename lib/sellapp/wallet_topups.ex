# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.WalletTopups do
  @moduledoc """
  Operations for the WalletTopups API.
  """

  @doc """
  Create a wallet top-up payment link

  Create a paid USD top-up for an existing customer and return data.checkout_url for the customer to pay. Creating a link leaves the wallet balance unchanged. Requires both wallet:write and charge API-key abilities and both store wallet and charge permissions. The wallet must be enabled and unfrozen, and the amount and selected method must satisfy the store's wallet settings. Payment completion credits the deposit and any bonus saved at creation exactly once; custom-method modifiers do not change the deposit. Browser returns and submitted payment proof do not prove payment. The return and cancel destination is the store's customer portal wallet. Currency, metadata, and redirect overrides are unsupported. Send an Idempotency-Key and reuse the same key and request for response replay within 24 hours. Changed input returns 409. For an uncertain provider outcome, inspect the charge before starting another top-up. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — path parameter
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateWalletTopUpResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create(client, customer, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCreateWalletTopUpRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/wallets/#{URI.encode(customer, &URI.char_unreserved?/1)}/top-ups",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "createWalletTopUp"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkCreateWalletTopUpResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateWalletTopUpResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
