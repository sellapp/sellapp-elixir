# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Refunds do
  @moduledoc """
  Operations for the Refunds API.
  """

  @doc """
  List refunds

  List refunds for orders and standalone charges in the selected store, one page at a time. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_refunds(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(SellApp.ListRefundsResponseValue200ApplicationJsonPropertyDataItem.t())}
          | {:error, SellApp.Error.error()}
  def list_refunds(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/refunds",
             params,
             Keyword.put(opts, :operation_id, "listRefunds")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list_refunds(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListRefundsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/refunds",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a refund

  Retrieve one store-scoped refund. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `refund` — The refund identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_refund(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetRefundResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_refund(client, refund, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/refunds/#{URI.encode(refund, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getRefund")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetRefundResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(body, &SellApp.SdkGetRefundResponseValue200ApplicationJson.from_map/1)

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
