# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.AddOnsParentProducts do
  @moduledoc """
  Operations for the AddOnsParentProducts API.
  """

  @doc """
  List an add-on's parent products

  Retrieve the ordered products, courses, and bookings that currently offer this add-on. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `addon` — The addon path parameter.
    * `params` — query parameters: `:limit`, `:page`, `:pagination`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(SellApp.SdkListAddOnSParentProductsResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def list(client, addon, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/addons/#{URI.encode(addon, &URI.char_unreserved?/1)}/parent-products",
             params,
             Keyword.put(opts, :operation_id, "listAddOnSParentProducts")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          addon,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkListAddOnSParentProductsResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/addons/#{URI.encode(addon, &URI.char_unreserved?/1)}/parent-products",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace an add-on's parent products

  Replace the complete ordered parent-product assignment. IDs must belong to the same store and may reference non-subscription products, courses, or bookings. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `addon` — The addon path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, addon, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceAddOnSParentProductsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/addons/#{URI.encode(addon, &URI.char_unreserved?/1)}/parent-products",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceAddOnSParentProducts"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceAddOnSParentProductsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
