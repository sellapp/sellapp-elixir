# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.TicketsMessages do
  @moduledoc """
  Operations for the TicketsMessages API.
  """

  @doc """
  List all ticket messages

  Read a ticket's conversation, 15 messages per page by default.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, ticket, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
             params,
             Keyword.put(opts, :operation_id, "listTicketMessages")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          ticket,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Reply to ticket

  Add a seller reply to a support conversation.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec reply(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplyToTicketResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def reply(client, ticket, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplyToTicketRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "replyToTicket")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplyToTicketResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplyToTicketResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve specific ticket message

  Retrieve one message using its ticket ID and message ID.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `message` — The message path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetTicketMessageResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, ticket, message, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/#{URI.encode(message, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getTicketMessage")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetTicketMessageResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetTicketMessageResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search ticket messages

  Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec search(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def search(client, ticket, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSearchTicketMessagesRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/search",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "searchTicketMessages"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        search(
          client,
          ticket,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v1/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List all ticket messages

  Read a ticket's conversation, 15 messages per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_list_ticket_messages(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def v2_list_ticket_messages(client, ticket, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
             params,
             Keyword.put(opts, :operation_id, "v2ListTicketMessages")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        v2_list_ticket_messages(
          client,
          ticket,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.V2ListTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Reply to ticket

  Add a seller reply to a support conversation. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_reply_to_ticket(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2ReplyToTicketResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_reply_to_ticket(client, ticket, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2ReplyToTicketRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "v2ReplyToTicket")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2ReplyToTicketResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2ReplyToTicketResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search ticket messages

  Search a ticket's messages using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_search_ticket_messages(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def v2_search_ticket_messages(client, ticket, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2SearchTicketMessagesRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/search",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2SearchTicketMessages"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        v2_search_ticket_messages(
          client,
          ticket,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.V2SearchTicketMessagesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve specific ticket message

  Retrieve one message using its ticket ID and message ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `ticket` — The ticket path parameter.
    * `message` — The message path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_get_ticket_message(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkV2GetTicketMessageResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_get_ticket_message(client, ticket, message, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/tickets/#{URI.encode(ticket, &URI.char_unreserved?/1)}/messages/#{URI.encode(message, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "v2GetTicketMessage")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2GetTicketMessageResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2GetTicketMessageResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
