# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.BundleItems do
  @moduledoc """
  Operations for the BundleItems API.
  """

  @doc """
  Retrieve a bundle item

  Retrieve a specific item from a bundle by its product variant ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `bundle` — The bundle path parameter.
    * `item` — The item path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetBundleItemResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, bundle, item, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/bundles/#{URI.encode(bundle, &URI.char_unreserved?/1)}/items/#{URI.encode(item, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getBundleItem")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetBundleItemResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetBundleItemResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List bundle items

  Retrieve a paginated list of product variants attached to a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `bundle` — The bundle path parameter.
    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, bundle, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/bundles/#{URI.encode(bundle, &URI.char_unreserved?/1)}/items",
             params,
             Keyword.put(opts, :operation_id, "listBundleItems")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          bundle,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/bundles/#{URI.encode(bundle, &URI.char_unreserved?/1)}/items",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Attach bundle items

  Attach one or more product variants to a bundle with quantities. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `bundle` — The bundle path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec attach(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkAttachBundleItemsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def attach(client, bundle, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkAttachBundleItemsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/bundles/#{URI.encode(bundle, &URI.char_unreserved?/1)}/items/attach",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "attachBundleItems"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkAttachBundleItemsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkAttachBundleItemsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Detach bundle items

  Detach one or more product variants from a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `bundle` — The bundle path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec detach(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkDetachBundleItemsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def detach(client, bundle, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkDetachBundleItemsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/bundles/#{URI.encode(bundle, &URI.char_unreserved?/1)}/items/detach",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "detachBundleItems"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkDetachBundleItemsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDetachBundleItemsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
