# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Entitlements do
  @moduledoc """
  Operations for the Entitlements API.
  """

  @doc """
  List customer entitlements

  Unified access without raw keys, serials, files, dynamic payloads, or seller-only metadata. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — The customer identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_customer_entitlements(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list_customer_entitlements(client, customer, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/customers/#{URI.encode(customer, &URI.char_unreserved?/1)}/entitlements",
             %{},
             Keyword.put(opts, :operation_id, "listCustomerEntitlements")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List customer entitlements

  Unified access without raw keys, serials, files, dynamic payloads, or seller-only metadata. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `external_id` — The externalId identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_customer_entitlements_by_external_id(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list_customer_entitlements_by_external_id(client, external_id, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/customers/external/#{URI.encode(external_id, &URI.char_unreserved?/1)}/entitlements",
             %{},
             Keyword.put(opts, :operation_id, "listCustomerEntitlementsByExternalId")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListCustomerEntitlementsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
