# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.StoreCustomDomains do
  @moduledoc """
  Operations for the StoreCustomDomains API.
  """

  @doc """
  List custom domains

  List the current store's custom-domain connection states and DNS verification records. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkListCustomDomainsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/store/custom-domains",
             %{},
             Keyword.put(opts, :operation_id, "listCustomDomains")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkListCustomDomainsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListCustomDomainsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Connect a custom domain

  Connect a hostname through SellApp's managed Cloudflare custom-hostname flow. IP addresses, wildcard domains, credentials, ports, queries, and fragments are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec connect(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkConnectCustomDomainResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def connect(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkConnectCustomDomainRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/store/custom-domains",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "connectCustomDomain"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkConnectCustomDomainResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkConnectCustomDomainResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a custom domain

  Retrieve a custom domain. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_domain` — The customDomain path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetCustomDomainResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, custom_domain, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/store/custom-domains/#{URI.encode(custom_domain, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getCustomDomain")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetCustomDomainResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetCustomDomainResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Disconnect a custom domain

  Disconnect a custom domain. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_domain` — The customDomain path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec disconnect(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDisconnectCustomDomainResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def disconnect(client, custom_domain, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/store/custom-domains/#{URI.encode(custom_domain, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "disconnectCustomDomain")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkDisconnectCustomDomainResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDisconnectCustomDomainResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Refresh custom domain status

  Ask Cloudflare to re-run custom-hostname validation and persist the latest DNS and SSL state. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `custom_domain` — The customDomain path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec refresh(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkRefreshCustomDomainStatusResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def refresh(client, custom_domain, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/store/custom-domains/#{URI.encode(custom_domain, &URI.char_unreserved?/1)}/refresh",
             %{},
             Keyword.put(opts, :operation_id, "refreshCustomDomainStatus")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkRefreshCustomDomainStatusResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkRefreshCustomDomainStatusResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
