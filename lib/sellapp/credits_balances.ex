# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CreditsBalances do
  @moduledoc """
  Operations for the CreditsBalances API.
  """

  @doc """
  List credit balances

  List store-scoped customer balances for dedicated credits products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`, `:pagination`, `:customer_id`, `:product_id`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.SdkListCreditBalancesResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/credit-balances",
             params,
             Keyword.put(opts, :operation_id, "listCreditBalances")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkListCreditBalancesResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/credit-balances",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a credit balance

  Retrieve one customer and credits-product balance with up to 100 recent ledger entries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — The customer path parameter.
    * `credit_product` — The creditProduct path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetCreditBalanceResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, customer, credit_product, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/credit-balances/#{URI.encode(customer, &URI.char_unreserved?/1)}/#{URI.encode(credit_product, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getCreditBalance")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetCreditBalanceResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetCreditBalanceResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
