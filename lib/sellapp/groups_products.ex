# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.GroupsProducts do
  @moduledoc """
  Operations for the GroupsProducts API.
  """

  @doc """
  Add products to group

  Add existing products to a group. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — The group path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec add(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkAddProductsToGroupResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def add(client, group, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkAddProductsToGroupRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products/attach",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "addProductsToGroup"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkAddProductsToGroupResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkAddProductsToGroupResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Remove products from group

  Remove products from a group without deleting the products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — The group path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec remove(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkRemoveProductsFromGroupResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def remove(client, group, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkRemoveProductsFromGroupRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products/detach",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "removeProductsFromGroup"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkRemoveProductsFromGroupResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkRemoveProductsFromGroupResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List all products within group

  List the products in a group, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — The group path parameter.
    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, group, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products",
             params,
             Keyword.put(opts, :operation_id, "listProductsWithinGroup")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          group,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace ordered group products

  Replace a group's complete product membership in one transaction. Requires the group API-key ability and store group permission. The order of product_ids becomes display order. An empty array clears the group. Missing or draft products return 422; products from another store are rejected. Retrying the same list has the same result. Private products remain assigned but are omitted from the returned group summary. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — path parameter
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkSyncGroupProductsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, group, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSyncGroupProductsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "syncGroupProducts"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkSyncGroupProductsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkSyncGroupProductsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List specific product within group

  Retrieve one product from a group. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — The group path parameter.
    * `product` — The product path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetProductWithinGroupResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, group, product, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products/#{URI.encode(product, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getProductWithinGroup")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetProductWithinGroupResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetProductWithinGroupResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search products within group

  Search the products in a group using JSON body filters, search terms, includes, and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `group` — The group path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec search(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def search(client, group, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSearchProductsWithinGroupRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products/search",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "searchProductsWithinGroup"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        search(
          client,
          group,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SearchProductsWithinGroupResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/groups/#{URI.encode(group, &URI.char_unreserved?/1)}/products/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
