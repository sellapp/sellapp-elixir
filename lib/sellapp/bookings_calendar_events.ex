# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.BookingsCalendarEvents do
  @moduledoc """
  Operations for the BookingsCalendarEvents API.
  """

  @doc """
  List booking date overrides

  List store-wide or variant-specific booking date overrides. Date boundaries are stored in UTC and interpreted in the booking timezone. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:product_variant_id`, `:from`, `:to`, `:status`, `:pagination`, `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(SellApp.SdkListBookingDateOverridesResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/booking-calendar-events",
             params,
             Keyword.put(opts, :operation_id, "listBookingDateOverrides")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkListBookingDateOverridesResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/booking-calendar-events",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Set booking date availability

  Atomically mark one or more local calendar dates unavailable or available for the store or one same-store booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec set(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkSetBookingDateAvailabilityResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def set(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSetBookingDateAvailabilityRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/booking-calendar-events",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "setBookingDateAvailability"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkSetBookingDateAvailabilityResponseValue201ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkSetBookingDateAvailabilityResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
