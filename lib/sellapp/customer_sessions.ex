# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CustomerSessions do
  @moduledoc """
  Operations for the CustomerSessions API.
  """

  @doc """
  Create a customer session

  Return a 15-minute bearer token once plus a separate single-use hosted redemption URL. Opening the URL signs the customer into this store's customer portal only. It does not sign them into the dashboard or other stores. Browser access and wallet credentials issued from it stop working when the session expires or is revoked. This response is never cached or replayed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create_customer_session(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateCustomerSessionResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create_customer_session(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :union,
        variants: [
          %{kind: :model, module: SellApp.CreateCustomerSessionRequestApplicationJsonOneOfValue1},
          %{kind: :model, module: SellApp.CreateCustomerSessionRequestApplicationJsonOneOfValue2}
        ],
        composition: :oneOf
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/customer-sessions",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "createCustomerSession"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkCreateCustomerSessionResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateCustomerSessionResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Revoke a customer session

  Revoke immediately. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `session` — The session identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec revoke_customer_session(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkRevokeCustomerSessionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def revoke_customer_session(client, session, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/customer-sessions/#{URI.encode(session, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "revokeCustomerSession")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkRevokeCustomerSessionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkRevokeCustomerSessionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
