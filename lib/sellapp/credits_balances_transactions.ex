# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CreditsBalancesTransactions do
  @moduledoc """
  Operations for the CreditsBalancesTransactions API.
  """

  @doc """
  List credit balance transactions

  Read all credit transactions for one customer and credits product in the selected store, newest first. Requires the credit API-key ability and store credit permission. Amounts are integer credit units. Use links.next to continue beyond the 100 entries embedded in balance details. An existing balance with no entries returns an empty data array; an unknown balance returns 404. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — path parameter
    * `credit_product` — path parameter
    * `params` — query parameters: `:page`, `:limit`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListCreditBalanceTransactionsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, customer, credit_product, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/credit-balances/#{URI.encode(customer, &URI.char_unreserved?/1)}/#{URI.encode(credit_product, &URI.char_unreserved?/1)}/transactions",
             params,
             Keyword.put(opts, :operation_id, "listCreditBalanceTransactions")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          customer,
          credit_product,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListCreditBalanceTransactionsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path:
            "/v2/credit-balances/#{URI.encode(customer, &URI.char_unreserved?/1)}/#{URI.encode(credit_product, &URI.char_unreserved?/1)}/transactions",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
