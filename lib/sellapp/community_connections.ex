# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.CommunityConnections do
  @moduledoc """
  Operations for the CommunityConnections API.
  """

  @doc """
  List community connections

  List Discord, Telegram, Slack, and WhatsApp connection and persisted health status for the authenticated store without contacting providers. Credentials and provider secrets are never returned. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkListCommunityConnectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/community-connections",
             %{},
             Keyword.put(opts, :operation_id, "listCommunityConnections")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkListCommunityConnectionsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListCommunityConnectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Start a community connection

  Start the provider-specific connection flow. OAuth providers return a connect URL containing encrypted state bound to the authenticated actor, store, platform, and purpose; that state expires after 15 minutes and securely authorizes the provider callback without forwarding the API bearer token or requiring a SellApp dashboard session. Open the URL exactly as issued, then poll the connection list with the original bearer token after the provider redirects. Do not append credentials, reconstruct, or reuse the URL. Legacy unsigned Slack URLs are rejected, so clients must call this endpoint again to restart those flows. Telegram returns a short-lived verification token, and WhatsApp returns a pollable QR session. The provider application must be configured, and Discord server connections require a connected Discord account. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `platform` — The community platform.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec start(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkStartCommunityConnectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def start(client, platform, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkStartCommunityConnectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/community-connections/#{URI.encode(platform, &URI.char_unreserved?/1)}/connect",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "startCommunityConnection"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkStartCommunityConnectionResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkStartCommunityConnectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Poll a community connection

  Poll a pending Telegram verification or WhatsApp QR connection using the encrypted status token returned by the connect operation. Tokens are bound to the authenticated store and initiating user. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `platform` — The community platform.
    * `params` — query parameters: `:status_token`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec poll(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkPollCommunityConnectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def poll(client, platform, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/community-connections/#{URI.encode(platform, &URI.char_unreserved?/1)}/status",
             params,
             Keyword.put(opts, :operation_id, "pollCommunityConnection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkPollCommunityConnectionResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkPollCommunityConnectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Complete a community connection

  Complete an authenticated WhatsApp QR connection by selecting a group returned by the status operation. The encrypted status token binds the session to the authenticated store and initiating user, and the server ID is revalidated against the provider response. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `platform` — The community platform.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec complete(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCompleteCommunityConnectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def complete(client, platform, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCompleteCommunityConnectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/community-connections/#{URI.encode(platform, &URI.char_unreserved?/1)}/complete",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "completeCommunityConnection"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkCompleteCommunityConnectionResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCompleteCommunityConnectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Verify a community connection

  Recheck provider and server health, then safely requeue eligible failed community grants. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `platform` — The community platform.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec verify(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkVerifyCommunityConnectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def verify(client, platform, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/community-connections/#{URI.encode(platform, &URI.char_unreserved?/1)}/verify",
             %{},
             Keyword.put(opts, :operation_id, "verifyCommunityConnection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkVerifyCommunityConnectionResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkVerifyCommunityConnectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Disconnect a community platform

  Idempotently disconnect the selected platform for the authenticated store and remove only that store's associated servers, channels, groups, or account links. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `platform` — The community platform.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec disconnect(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDisconnectCommunityPlatformResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def disconnect(client, platform, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/community-connections/#{URI.encode(platform, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "disconnectCommunityPlatform")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkDisconnectCommunityPlatformResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDisconnectCommunityPlatformResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
