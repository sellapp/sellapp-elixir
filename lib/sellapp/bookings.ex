# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Bookings do
  @moduledoc """
  Operations for the Bookings API.
  """

  @doc """
  List appointments

  List appointments for the selected store. Requires the `invoice` Sanctum ability and invoice permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`, `:pagination`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.SdkListAppointmentsResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/bookings",
             params,
             Keyword.put(opts, :operation_id, "listAppointments")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkListAppointmentsResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/bookings",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search appointments

  Search appointment UUIDs or customer email addresses and compose store-scoped filters and sorting. The public `id` filter targets the UUID returned as `id`; `uuid` remains an equivalent compatibility filter. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec search(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.SdkSearchAppointmentsResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def search(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSearchAppointmentsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/bookings/search",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "searchAppointments"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        search(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkSearchAppointmentsResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/bookings/search",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve an appointment

  Retrieve a store-scoped appointment by UUID or numeric ID. Provider error details are intentionally not exposed. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `booking` — The appointment UUID or numeric ID.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetAppointmentResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, booking, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/bookings/#{URI.encode(booking, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getAppointment")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetAppointmentResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetAppointmentResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update an appointment

  Reschedule an appointment atomically. Current availability is verified under the same conflict lock used by checkout holds. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `booking` — The appointment UUID or numeric ID.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateAppointmentResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, booking, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateAppointmentRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/bookings/#{URI.encode(booking, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateAppointment"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkUpdateAppointmentResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateAppointmentResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Cancel an appointment

  Cancel a store-owned appointment. Cancellation is the only supported administrative status transition. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `booking` — The appointment UUID or numeric ID.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec cancel(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCancelAppointmentResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def cancel(client, booking, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCancelAppointmentRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/bookings/#{URI.encode(booking, &URI.char_unreserved?/1)}/status",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "cancelAppointment"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkCancelAppointmentResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCancelAppointmentResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
