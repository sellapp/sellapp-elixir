# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Cast do
  @moduledoc false
  def drop_nils(map), do: Map.reject(map, fn {_key, value} -> is_nil(value) end)

  def keep_present(map, required, present),
    do:
      Map.reject(map, fn {key, value} ->
        is_nil(value) and key not in required and key not in present
      end)

  def nested(nil, _caster), do: nil
  def nested(value, caster), do: caster.(value)
  def enum(nil, _caster), do: nil
  def enum(value, caster), do: caster.(value)
  def list(nil, _caster), do: nil
  def list(values, caster) when is_list(values), do: Enum.map(values, caster)
  def map_values(nil, _caster), do: nil

  def map_values(values, caster) when is_map(values),
    do: Map.new(values, fn {key, value} -> {key, caster.(value)} end)

  def dump_struct(nil, _dumper), do: nil
  def dump_struct(value, dumper), do: dumper.(value)
  def dump_union(%module{} = value), do: module.to_map(value)
  def dump_union(value), do: value

  def discriminated(nil, _property, _casters), do: nil

  def discriminated(value, property, casters) when is_map(value) do
    case Map.get(casters, Map.get(value, property)) do
      nil -> value
      caster -> caster.(value)
    end
  end
end
