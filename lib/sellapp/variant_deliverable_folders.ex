# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.VariantDeliverableFolders do
  @moduledoc """
  Operations for the VariantDeliverableFolders API.
  """

  @doc """
  List variant deliverable folders

  List variant deliverable folders. Requires the listing Sanctum ability. Returns folders ordered by sort order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkListVariantDeliverableFoldersResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, product, variant, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders",
             %{},
             Keyword.put(opts, :operation_id, "listVariantDeliverableFolders")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkListVariantDeliverableFoldersResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListVariantDeliverableFoldersResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a variant deliverable folder

  Create a variant deliverable folder. Requires the listing Sanctum ability. Parent folders are validated against the same variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create(client, product, variant, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCreateVariantDeliverableFolderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "createVariantDeliverableFolder"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateVariantDeliverableFolderResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a variant deliverable folder

  Retrieve a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `folder` — The deliverable folder identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetVariantDeliverableFolderResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, product, variant, folder, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders/#{URI.encode(folder, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getVariantDeliverableFolder")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkGetVariantDeliverableFolderResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetVariantDeliverableFolderResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace variant deliverable folder settings

  Replace variant deliverable folder settings. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `folder` — The deliverable folder identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, product, variant, folder, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :model,
        module: SellApp.SdkReplaceVariantDeliverableFolderSettingsRequestApplicationJson
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders/#{URI.encode(folder, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceVariantDeliverableFolderSettings"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module:
            SellApp.SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceVariantDeliverableFolderSettingsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a variant deliverable folder

  Update a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `folder` — The deliverable folder identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, product, variant, folder, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateVariantDeliverableFolderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders/#{URI.encode(folder, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateVariantDeliverableFolder"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateVariantDeliverableFolderResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a variant deliverable folder

  Delete a variant deliverable folder. Requires the listing Sanctum ability. Folder and parent IDs are scoped through the authenticated store, product, and variant; cycles are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `folder` — The deliverable folder identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDeleteVariantDeliverableFolderResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def delete(client, product, variant, folder, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/folders/#{URI.encode(folder, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteVariantDeliverableFolder")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkDeleteVariantDeliverableFolderResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDeleteVariantDeliverableFolderResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
