# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Sections do
  @moduledoc """
  Operations for the Sections API.
  """

  @doc """
  List all sections

  List your storefront sections, 15 per page by default.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(SellApp.ListSectionsResponseValue200ApplicationJsonPropertyDataItem.t())}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/sections",
             params,
             Keyword.put(opts, :operation_id, "listSections")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListSectionsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v1/sections",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a section

  Create a section to organize your storefront.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateSectionResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCreateSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/sections",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "createSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkCreateSectionResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateSectionResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a section

  Retrieve a section by its ID.

  ## Parameters

    * `section` — The section path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, section, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a section

  Update a storefront section's details.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "replaceSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a section

  Update a storefront section's details.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "updateSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkUpdateSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a section

  Deletes a section. This will permanently delete the section and its details.

  ## Parameters

    * `section` — The section path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDeleteSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def delete(client, section, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkDeleteSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDeleteSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section order

  Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace_order(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceSectionOrderResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace_order(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceSectionOrderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v1/sections/order",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceSectionOrder"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceSectionOrderResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceSectionOrderResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section products

  Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace_products(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceSectionProductsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace_products(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceSectionOrderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}/products",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceSectionProducts"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceSectionProductsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceSectionProductsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section groups

  Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace_groups(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceSectionGroupsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace_groups(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceSectionOrderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v1/sections/#{URI.encode(section, &URI.char_unreserved?/1)}/groups",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceSectionGroups"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceSectionGroupsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceSectionGroupsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search sections

  Search sections using JSON body filters, search terms, includes, and sort instructions.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec search(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.SearchSectionsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def search(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSearchSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/sections/search",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "searchSections")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        search(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SearchSectionsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v1/sections/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch create sections

  Create multiple sections in one request by sending a `resources` array.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec batch_create(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkBatchCreateSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def batch_create(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkBatchCreateSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/sections/batch",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "batchCreateSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkBatchCreateSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkBatchCreateSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch update sections

  Update multiple sections in one request by sending a `resources` object keyed by section ID.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec batch_update(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkBatchUpdateSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def batch_update(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkBatchUpdateSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v1/sections/batch",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "batchUpdateSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkBatchUpdateSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkBatchUpdateSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch delete sections

  Delete multiple sections in one request by sending the section IDs in `resources`.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec batch_delete(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkBatchDeleteSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def batch_delete(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkBatchDeleteCouponsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v1/sections/batch",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "batchDeleteSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkBatchDeleteSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkBatchDeleteSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List all sections

  List your storefront sections, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_list_sections(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def v2_list_sections(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/sections",
             params,
             Keyword.put(opts, :operation_id, "v2ListSections")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        v2_list_sections(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.V2ListSectionsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/sections",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a section

  Create a section to organize your storefront. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_create_section(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2CreateSectionResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_create_section(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2CreateSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/sections",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2CreateSection"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2CreateSectionResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2CreateSectionResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search sections

  Search sections using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_search_sections(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def v2_search_sections(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2SearchSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/sections/search",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2SearchSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        v2_search_sections(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.V2SearchSectionsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/sections/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a section

  Retrieve a section by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_get_section(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkV2GetSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_get_section(client, section, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "v2GetSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2GetSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2GetSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a section

  Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_replace_section(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2ReplaceSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_replace_section(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2ReplaceSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2ReplaceSection"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2ReplaceSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2ReplaceSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a section

  Update a storefront section's details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_update_section(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2UpdateSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_update_section(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2UpdateSectionRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "v2UpdateSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2UpdateSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2UpdateSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a section

  Deletes a section. This will permanently delete the section and its details. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_delete_section(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkV2DeleteSectionResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_delete_section(client, section, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "v2DeleteSection")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2DeleteSectionResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2DeleteSectionResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch create sections

  Create multiple sections in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_batch_create_sections(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2BatchCreateSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_batch_create_sections(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2BatchCreateSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/sections/batch",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2BatchCreateSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2BatchCreateSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2BatchCreateSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch update sections

  Update multiple sections in one request by sending a `resources` object keyed by section ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_batch_update_sections(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2BatchUpdateSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_batch_update_sections(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2BatchUpdateSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/sections/batch",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2BatchUpdateSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2BatchUpdateSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2BatchUpdateSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Batch delete sections

  Delete multiple sections in one request by sending the section IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_batch_delete_sections(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2BatchDeleteSectionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_batch_delete_sections(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2BatchDeleteSectionsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/sections/batch",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2BatchDeleteSections"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2BatchDeleteSectionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2BatchDeleteSectionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section order

  Replace the storefront section order with every section ID in the store. IDs are applied in array order; incomplete lists and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_replace_section_order(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2ReplaceSectionOrderResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_replace_section_order(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2ReplaceSectionOrderRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/sections/order",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2ReplaceSectionOrder"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2ReplaceSectionOrderResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2ReplaceSectionOrderResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section products

  Replace the products assigned to a section. Product IDs are stored in the supplied order; omitted products are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_replace_section_products(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2ReplaceSectionProductsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_replace_section_products(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2ReplaceSectionProductsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}/products",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2ReplaceSectionProducts"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkV2ReplaceSectionProductsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2ReplaceSectionProductsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace section groups

  Replace the groups assigned to a section. Group IDs are stored in the supplied order; omitted groups are detached and unknown IDs are rejected. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `section` — The section path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_replace_section_groups(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def v2_replace_section_groups(client, section, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkV2ReplaceSectionGroupsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/sections/#{URI.encode(section, &URI.char_unreserved?/1)}/groups",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "v2ReplaceSectionGroups"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkV2ReplaceSectionGroupsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
