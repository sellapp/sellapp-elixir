# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.VariantDeliverables do
  @moduledoc """
  Operations for the VariantDeliverables API.
  """

  @doc """
  Retrieve variant deliverable configuration

  Retrieve variant deliverable configuration. Requires the listing Sanctum ability. Returns delivery types and safe configuration for a variant in the authenticated store. Dynamic endpoint URLs and inventory data are not returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, product, variant, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable",
             %{},
             Keyword.put(opts, :operation_id, "getVariantDeliverableConfiguration")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetVariantDeliverableConfigurationResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace variant deliverable configuration

  Replace variant deliverable configuration. Requires the listing Sanctum ability. Replaces this resource's delivery types atomically. Pass expected_updated_at from the product variant resource to reject stale updates with 422. TEXT, BOOKING, CREDITS, and BUNDLE must remain unchanged. Removing DOWNLOADABLE deletes managed files and folders after commit; removing a type clears its private configuration. Dynamic endpoints are write-only. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, product, variant, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :model,
        module: SellApp.SdkReplaceVariantDeliverableConfigurationRequestApplicationJson
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceVariantDeliverableConfiguration"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceVariantDeliverableConfigurationResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
