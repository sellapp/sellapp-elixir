# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.LineItems do
  @moduledoc """
  Operations for the LineItems API.
  """

  @doc """
  List order line items

  List purchase line items for the selected store. Requires the `invoice` API-key ability or the corresponding OAuth scope and current store permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListOrderLineItemsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/line-items",
             params,
             Keyword.put(opts, :operation_id, "listOrderLineItems")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListOrderLineItemsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/line-items",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Search order line items

  Search product-level sale records with filters and sorting. Requires the `invoice` token ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec search(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.SearchOrderLineItemsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def search(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkSearchOrderLineItemsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/line-items/search",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "searchOrderLineItems"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        search(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SearchOrderLineItemsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/line-items/search",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve an order line item

  Retrieve one product-level sale record for the selected store. Requires the `invoice` token ability. Records from another store or whose order does not match the store return `404`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `line_item` — The purchase line-item ID.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetOrderLineItemResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, line_item, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/line-items/#{URI.encode(line_item, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getOrderLineItem")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetOrderLineItemResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetOrderLineItemResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
