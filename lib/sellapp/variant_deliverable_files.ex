# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.VariantDeliverableFiles do
  @moduledoc """
  Operations for the VariantDeliverableFiles API.
  """

  @doc """
  List variant deliverable files

  List variant deliverable files. Requires the listing Sanctum ability. Lists managed files without revealing storage keys. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, product, variant, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files",
             params,
             Keyword.put(opts, :operation_id, "listVariantDeliverableFiles")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          product,
          variant,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListVariantDeliverableFilesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path:
            "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Upload a variant deliverable file

  Upload a variant deliverable file. Requires the listing Sanctum ability. Accepts an uploaded file, stores it under a server-generated key, and queues the durable move after commit. Uploads may be staged before DOWNLOADABLE is activated; inactive pending files are retained temporarily and scheduled for cleanup if activation is abandoned. Client-supplied storage paths are not accepted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec upload(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUploadVariantDeliverableFileResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def upload(client, product, variant, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUploadVariantDeliverableFileRequestMultipartFormData},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :request_body, true), :body_encoding, :multipart),
               :operation_id,
               "uploadVariantDeliverableFile"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkUploadVariantDeliverableFileResponseValue201ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUploadVariantDeliverableFileResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a variant deliverable file

  Retrieve a variant deliverable file. Requires the listing Sanctum ability. The file is scoped through the authenticated store, product, and variant. Internal storage keys are immutable and never returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `file` — The managed deliverable file identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetVariantDeliverableFileResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, product, variant, file, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files/#{URI.encode(file, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getVariantDeliverableFile")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkGetVariantDeliverableFileResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetVariantDeliverableFileResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace variant deliverable file settings

  Replace variant deliverable file settings. Requires the listing Sanctum ability. The file is scoped through the authenticated store, product, and variant. Internal storage keys are immutable and never returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `file` — The managed deliverable file identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, product, variant, file, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :model,
        module: SellApp.SdkReplaceVariantDeliverableFileSettingsRequestApplicationJson
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files/#{URI.encode(file, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceVariantDeliverableFileSettings"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceVariantDeliverableFileSettingsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a variant deliverable file

  Update a variant deliverable file. Requires the listing Sanctum ability. The file is scoped through the authenticated store, product, and variant. Internal storage keys are immutable and never returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `file` — The managed deliverable file identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, product, variant, file, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateVariantDeliverableFileRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files/#{URI.encode(file, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateVariantDeliverableFile"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateVariantDeliverableFileResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a variant deliverable file

  Delete a variant deliverable file. Requires the listing Sanctum ability. The file is scoped through the authenticated store, product, and variant. Internal storage keys are immutable and never returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product identifier or slug. The product must belong to the authenticated store.
    * `variant` — The product variant identifier.
    * `file` — The managed deliverable file identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDeleteVariantDeliverableFileResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def delete(client, product, variant, file, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/variants/#{URI.encode(variant, &URI.char_unreserved?/1)}/deliverable/files/#{URI.encode(file, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteVariantDeliverableFile")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkDeleteVariantDeliverableFileResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDeleteVariantDeliverableFileResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
