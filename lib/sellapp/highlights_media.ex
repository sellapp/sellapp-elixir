# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.HighlightsMedia do
  @moduledoc """
  Operations for the HighlightsMedia API.
  """

  @doc """
  List highlight media

  List media attached to a highlight in display order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkListHighlightMediaResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, highlight, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media",
             %{},
             Keyword.put(opts, :operation_id, "listHighlightMedia")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkListHighlightMediaResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListHighlightMediaResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Add highlight media

  Upload an image or video and append it to a highlight, up to 30 media items in total. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec add(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkAddHighlightMediaResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def add(client, highlight, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkAddHighlightMediaRequestMultipartFormData},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :request_body, true), :body_encoding, :multipart),
               :operation_id,
               "addHighlightMedia"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkAddHighlightMediaResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkAddHighlightMediaResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Reorder highlight media

  Replace the complete media order for a highlight with the supplied media IDs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec reorder(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReorderHighlightMediaResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def reorder(client, highlight, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReorderHighlightsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media/reorder",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "reorderHighlightMedia"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReorderHighlightMediaResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReorderHighlightMediaResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace highlight media

  Replace highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `media` — The media path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceHighlightMediaResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, highlight, media, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceHighlightMediaRequestMultipartFormData},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media/#{URI.encode(media, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :request_body, true), :body_encoding, :multipart),
               :operation_id,
               "replaceHighlightMedia"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceHighlightMediaResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceHighlightMediaResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update highlight media

  Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `media` — The media path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace_metadata(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace_metadata(client, highlight, media, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceHighlightMediaMetadataRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media/#{URI.encode(media, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceHighlightMediaMetadata"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceHighlightMediaMetadataResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update highlight media

  Update highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `media` — The media path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateHighlightMediaResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update(client, highlight, media, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateHighlightMediaRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media/#{URI.encode(media, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateHighlightMedia"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkUpdateHighlightMediaResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateHighlightMediaResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete highlight media

  Delete highlight media for a highlight. Deleting the last media item unpublishes the highlight. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `highlight` — The highlight path parameter.
    * `media` — The media path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkDeleteHighlightMediaResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def delete(client, highlight, media, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/highlights/#{URI.encode(highlight, &URI.char_unreserved?/1)}/media/#{URI.encode(media, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteHighlightMedia")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkDeleteHighlightMediaResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkDeleteHighlightMediaResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
