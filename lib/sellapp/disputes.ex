# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Disputes do
  @moduledoc """
  Operations for the Disputes API.
  """

  @doc """
  List disputes

  Read-only, redacted, store-scoped disputes that may reference an Order or Charge. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_disputes(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(SellApp.ListDisputesResponseValue200ApplicationJsonPropertyDataItem.t())}
          | {:error, SellApp.Error.error()}
  def list_disputes(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/disputes",
             params,
             Keyword.put(opts, :operation_id, "listDisputes")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list_disputes(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListDisputesResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/disputes",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a dispute

  Retrieve one redacted dispute. No dispute mutations are exposed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `dispute` — The dispute identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_dispute(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetDisputeResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_dispute(client, dispute, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/disputes/#{URI.encode(dispute, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getDispute")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetDisputeResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetDisputeResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
