# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PromotionsPhases do
  @moduledoc """
  Operations for the PromotionsPhases API.
  """

  @doc """
  List promotion phases

  List a promotion's active phases in evaluation order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `promotion` — The promotion path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkListPromotionPhasesResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, promotion, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/promotions/#{URI.encode(promotion, &URI.char_unreserved?/1)}/phases",
             %{},
             Keyword.put(opts, :operation_id, "listPromotionPhases")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkListPromotionPhasesResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListPromotionPhasesResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace promotion phases

  Replace the complete phase set. Array order becomes phase position; at least one phase is required. Historical referenced phases remain soft-deleted. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `promotion` — The promotion path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplacePromotionPhasesResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, promotion, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplacePromotionPhasesRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/promotions/#{URI.encode(promotion, &URI.char_unreserved?/1)}/phases",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replacePromotionPhases"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplacePromotionPhasesResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplacePromotionPhasesResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
