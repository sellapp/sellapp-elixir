# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Blacklists do
  @moduledoc """
  Operations for the Blacklists API.
  """

  @doc """
  List all blacklist rules

  Retrieve a paginated list of blacklist rules for your store. By default, a maximum of fifteen blacklist rules are returned per page.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.Blacklist.t())} | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/blacklists",
             params,
             Keyword.put(opts, :operation_id, "listBlacklists")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.Blacklist.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v1/blacklists",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a blacklist rule

  Create a new blacklist rule for your store.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def create(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.BlacklistCreateRequest},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v1/blacklists",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "createBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a blacklist rule

  Retrieve a specific blacklist rule by its unique identifier.

  ## Parameters

    * `blacklist` — The unique identifier of the blacklist rule.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def get(client, blacklist, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v1/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a blacklist rule

  Update one or more attributes on an existing blacklist rule.

  ## Parameters

    * `blacklist` — The unique identifier of the blacklist rule.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def update(client, blacklist, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.BlacklistUpdateRequest},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v1/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "updateBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a blacklist rule

  Permanently delete a blacklist rule from your store.

  ## Parameters

    * `blacklist` — The unique identifier of the blacklist rule.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec delete(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def delete(client, blacklist, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v1/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "deleteBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List blacklist rules

  List store-scoped blacklist rules. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_list_blacklists(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.Blacklist.t())} | {:error, SellApp.Error.error()}
  def v2_list_blacklists(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/blacklists",
             params,
             Keyword.put(opts, :operation_id, "v2ListBlacklists")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        v2_list_blacklists(
          client,
          SellApp.Page.next_params(params, "after", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.Blacklist.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/blacklists",
          pagination_param: "after"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a blacklist rule

  Create a store-scoped blacklist rule. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_create_blacklist(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def v2_create_blacklist(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.BlacklistCreateRequest},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/blacklists",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2CreateBlacklist"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a blacklist rule

  Retrieve a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `blacklist` — The blacklist identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_get_blacklist(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def v2_get_blacklist(client, blacklist, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "v2GetBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace a blacklist rule

  Replace a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `blacklist` — The blacklist identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_replace_blacklist(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def v2_replace_blacklist(client, blacklist, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.BlacklistUpdateRequest},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2ReplaceBlacklist"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a blacklist rule

  Update a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `blacklist` — The blacklist identifier.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_update_blacklist(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def v2_update_blacklist(client, blacklist, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.BlacklistUpdateRequest},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "v2UpdateBlacklist"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Delete a blacklist rule

  Delete a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `blacklist` — The blacklist identifier.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec v2_delete_blacklist(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.BlacklistResponse.t()} | {:error, SellApp.Error.error()}
  def v2_delete_blacklist(client, blacklist, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :delete,
             "/v2/blacklists/#{URI.encode(blacklist, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "v2DeleteBlacklist")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.BlacklistResponse},
        :response,
        "$response"
      )

      result = SellApp.Cast.nested(body, &SellApp.BlacklistResponse.from_map/1)
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
