# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Account do
  @moduledoc """
  Operations for the Account API.
  """

  @doc """
  Read your profile

  Read your account identity and credential type with account:read on an API key or the official CLI OAuth grant. No store or X-STORE is needed, including before creating your first store. Browser and customer sessions are rejected.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_account(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkGetAccountResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_account(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/me",
             %{},
             Keyword.put(opts, :operation_id, "getAccount")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetAccountResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetAccountResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List accessible stores

  List all stores you can currently access, including stores created or joined after login. API keys require account:read; selected-store keys see only the intersection of current membership and their fixed store list. An empty list is a successful result. Use a returned slug in X-STORE for store operations.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_stores(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkListStoresResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list_stores(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/stores",
             %{},
             Keyword.put(opts, :operation_id, "listStores")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkListStoresResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListStoresResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a store

  Create a store owned by the authenticated user, with the same defaults as dashboard creation. Requires stores:create on an unrestricted user-owned API key, or the official CLI OAuth admin grant. Customer and browser sessions and direct MCP bearer tokens are rejected; MCP uses reviewed server delegation. Selected-store keys cannot create stores. No existing store or X-STORE header is needed; Name and slug are required; slug is normalized to lowercase. Visibility defaults to PUBLIC. This does not change your dashboard's selected store. Idempotency-Key is required and retained for 24 hours: reuse the same key and identical body after a lost response. A conflicting request returns 409; invalid or duplicate slugs return 422. Retries are bound to the user and operation across token rotation and supported credential types; current authorization is checked before replay. Store defaults and the successful retry receipt commit together. You can create one new store per user every 60 seconds, across all credentials. A new creation during that interval returns 429 with Retry-After and rate-limit headers. Wait for that delay and retry the same key and body. Replaying a successful original key recovers its store without creating another or consuming another creation allowance.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec create_store(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkCreateStoreResponseValue201ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def create_store(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkCreateStoreRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/stores",
             params,
             Keyword.put(
               Keyword.put(Keyword.put(opts, :idempotency_supported, true), :request_body, true),
               :operation_id,
               "createStore"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkCreateStoreResponseValue201ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreateStoreResponseValue201ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Read an accessible store

  Read a currently accessible store by numeric ID with account:read on an API key or official CLI OAuth. No X-STORE is needed. Keys with selected stores must include this ID. Inaccessible stores return 404.

  ## Parameters

    * `store` — path parameter
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_store(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetStoreResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_store(client, store, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/stores/#{URI.encode(store, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getStore")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetStoreResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(body, &SellApp.SdkGetStoreResponseValue200ApplicationJson.from_map/1)

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Inspect effective permissions

  Inspect authorization for the explicit X-STORE slug. API keys require account:read and return key_abilities, store_permissions, effective_abilities and store_access. OAuth returns its granted scopes and reviewed permission categories. The credential_type identifies the typed response. These checks do not establish record-state, provider or financial eligibility.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_store_permissions(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkGetStorePermissionsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_store_permissions(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/permissions",
             %{},
             Keyword.put(opts, :operation_id, "getStorePermissions")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetStorePermissionsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetStorePermissionsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
