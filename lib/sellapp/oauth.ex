# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.OAuth do
  @moduledoc """
  Operations for the Oauth API.
  """

  @doc """
  Read OAuth server metadata

  Discover the official CLI authorization, token and revocation endpoints and supported scopes. The CLI uses public client authentication without a secret.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_oauth_authorization_server_metadata(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get_oauth_authorization_server_metadata(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/.well-known/oauth-authorization-server",
             %{},
             Keyword.put(opts, :operation_id, "getOAuthAuthorizationServerMetadata")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Review CLI authorization

  Open in the user's browser to authorize the official CLI using existing store permissions. Only the configured public CLI registration is supported. PKCE S256 and non-empty state are required. The callback may vary only the port of the registered numeric loopback URL; localhost, wildcard hosts, fragments and non-loopback HTTP are rejected. Unsupported clients receive invalid_client and unsupported scopes receive invalid_scope.

  ## Parameters

    * `params` — query parameters: `:response_type`, `:client_id`, `:redirect_uri`, `:scope`, `:state`, `:code_challenge`, `:code_challenge_method`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get_oauth_authorization_request(SellApp.Client.t(), map(), keyword()) ::
          {:ok, String.t()} | {:error, SellApp.Error.error()}
  def get_oauth_authorization_request(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/oauth/authorize",
             params,
             Keyword.put(opts, :operation_id, "getOAuthAuthorizationRequest")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body
      SellApp.Schema.validate_value!(body, %{kind: :string}, :response, "$response")
      result = body
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Approve CLI access

  Submit the official CLI consent form in the same logged-in browser session. The admin grant covers every store you can access now or later, including when you currently have no stores. It also permits store creation. Local agents connected through the CLI share this connection. Approval never adds store role permissions. Current client eligibility, membership and permissions are rechecked, including for stale consent.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec approve_oauth_authorization(SellApp.Client.t(), map(), keyword()) ::
          {:ok, term()} | {:error, SellApp.Error.error()}
  def approve_oauth_authorization(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :model,
        module: SellApp.SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded
      },
      :request,
      "$request.body"
    )

    SellApp.Client.request(
      client,
      :post,
      "/oauth/authorize",
      params,
      Keyword.put(
        Keyword.put(opts, :request_body, true),
        :operation_id,
        "approveOAuthAuthorization"
      )
    )
  end

  @doc """
  Deny CLI access

  Submit the denial form from the same browser session. The callback receives access_denied and the original state.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec deny_oauth_authorization(SellApp.Client.t(), map(), keyword()) ::
          {:ok, term()} | {:error, SellApp.Error.error()}
  def deny_oauth_authorization(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :model,
        module: SellApp.SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded
      },
      :request,
      "$request.body"
    )

    SellApp.Client.request(
      client,
      :delete,
      "/oauth/authorize",
      params,
      Keyword.put(Keyword.put(opts, :request_body, true), :operation_id, "denyOAuthAuthorization")
    )
  end

  @doc """
  Exchange or refresh OAuth tokens

  Exchange an official CLI authorization code with the original redirect URI and PKCE verifier, or rotate a refresh token. Send the configured client_id without a secret. Client eligibility and current grant validity are rechecked. Access tokens last 60 minutes; refresh token families last 90 days. Atomically replace rotated refresh tokens. Reuse may revoke the family; reauthorize after invalid_grant. Fix registration or scope configuration for invalid_client or invalid_scope instead of repeating login.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec exchange_oauth_token(SellApp.Client.t(), map(), keyword()) ::
          {:ok, SellApp.SdkExchangeOAuthTokenResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def exchange_oauth_token(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :union,
        variants: [
          %{
            kind: :model,
            module: SellApp.ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1
          },
          %{
            kind: :model,
            module: SellApp.ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2
          }
        ],
        composition: :oneOf
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/oauth/token",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "exchangeOAuthToken"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkExchangeOAuthTokenResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkExchangeOAuthTokenResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Revoke an OAuth token

  Revoke the matching personal CLI installation and all its token families. Send the configured public client_id without a secret. Unknown tokens also succeed without disclosing whether a token existed.

  ## Parameters

    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec revoke_oauth_token(SellApp.Client.t(), map(), keyword()) ::
          {:ok, [term()]} | {:error, SellApp.Error.error()}
  def revoke_oauth_token(client, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/oauth/revoke",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "revokeOAuthToken"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :array, items: %{kind: :any}, max_items: 0},
        :response,
        "$response"
      )

      result = body
      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
