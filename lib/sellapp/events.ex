# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Events do
  @moduledoc """
  Operations for the Events API.
  """

  @doc """
  List integration events

  Poll the 30-day journal with signed, filter-bound cursors. Use client-controlled backoff between ordinary requests. An expired position returns 410 cursor_expired with cursor_polling guidance. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:cursor`, `:limit`, `:type`, `:subject_type`, `:subject_id`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_integration_events(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list_integration_events(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/events",
             params,
             Keyword.put(opts, :operation_id, "listIntegrationEvents")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list_integration_events(
          client,
          SellApp.Page.next_params(params, "cursor", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListIntegrationEventsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/events",
          pagination_param: "cursor"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  List order events

  Read retained events for this order. Use the returned cursor for the next request and wait between polls. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `order` — The order identifier.
    * `params` — query parameters: `:cursor`, `:limit`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list_order_events(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list_order_events(client, order, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/orders/#{URI.encode(order, &URI.char_unreserved?/1)}/events",
             params,
             Keyword.put(opts, :operation_id, "listOrderEvents")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list_order_events(
          client,
          order,
          SellApp.Page.next_params(params, "cursor", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListOrderEventsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/orders/#{URI.encode(order, &URI.char_unreserved?/1)}/events",
          pagination_param: "cursor"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
