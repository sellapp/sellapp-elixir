# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.PaymentMethods do
  @moduledoc """
  Operations for the PaymentMethods API.
  """

  @doc """
  List payment methods

  List safe, pollable setup and checkout status for every payment method available to the store. Provider credentials and wallet addresses are never serialized. Requires the `payment-method` ability or the stronger legacy `store` ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), keyword()) ::
          {:ok, SellApp.SdkListPaymentMethodsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def list(client, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/payment-methods",
             %{},
             Keyword.put(opts, :operation_id, "listPaymentMethods")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkListPaymentMethodsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkListPaymentMethodsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve payment method status

  Poll one payment method's connection, health, required-input, and checkout-enabled state. No stored secret is returned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `payment_method` — Uppercase payment method identifier returned by the collection endpoint.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetPaymentMethodStatusResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, payment_method, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/payment-methods/#{URI.encode(payment_method, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getPaymentMethodStatus")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetPaymentMethodStatusResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetPaymentMethodStatusResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Enable or disable a payment method

  Change checkout availability after setup has succeeded. Enabling a disconnected method returns an actionable validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `payment_method` — Uppercase payment method identifier returned by the collection endpoint.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec enable(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def enable(client, payment_method, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkEnableOrDisablePaymentMethodRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/payment-methods/#{URI.encode(payment_method, &URI.char_unreserved?/1)}",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "enableOrDisablePaymentMethod"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkEnableOrDisablePaymentMethodResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Create a payment connection handoff

  Return a provider-hosted connect URL and a status URL to poll. Currently supported for Stripe and PayPal; unsupported methods return a validation error. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `payment_method` — Uppercase payment method identifier returned by the collection endpoint.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec connect(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def connect(client, payment_method, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/payment-methods/#{URI.encode(payment_method, &URI.char_unreserved?/1)}/connect",
             %{},
             Keyword.put(opts, :operation_id, "createPaymentConnectionHandoff")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkCreatePaymentConnectionHandoffResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Validate and save payment method configuration

  Validate provider credentials or crypto wallet input with the same setup services used by the dashboard, then save the configuration. Secret fields are write-only and never appear in responses or audit payloads. Supported methods are NMI, Mercado Pago, Razorpay, Mollie, LiFi, and Bitcart-backed crypto wallets. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `payment_method` — Uppercase payment method identifier returned by the collection endpoint.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec validate(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok,
           SellApp.SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def validate(client, payment_method, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{
        kind: :union,
        variants: [
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue1
          },
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue2
          },
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue3
          },
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue4
          },
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue5
          },
          %{
            kind: :model,
            module:
              SellApp.ValidateAndSavePaymentMethodConfigurationRequestApplicationJsonOneOfValue6
          }
        ],
        composition: :oneOf
      },
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/payment-methods/#{URI.encode(payment_method, &URI.char_unreserved?/1)}/configuration",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "validateAndSavePaymentMethodConfiguration"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module:
            SellApp.SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkValidateAndSavePaymentMethodConfigurationResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
