# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.ProductsAddons do
  @moduledoc """
  Operations for the ProductsAddons API.
  """

  @doc """
  List a product's add-ons

  Retrieve the ordered add-ons assigned to a product, course, or booking. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product path parameter.
    * `params` — query parameters: `:limit`, `:page`, `:pagination`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.Page.t(SellApp.SdkListProductSAddOnsResponseValue200ApplicationJson.t())}
          | {:error, SellApp.Error.error()}
  def list(client, product, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/addons",
             params,
             Keyword.put(opts, :operation_id, "listProductSAddOns")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          product,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.SdkListProductSAddOnsResponseValue200ApplicationJson.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/addons",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Replace a product's add-ons

  Replace the complete ordered add-on assignment for a non-subscription product, course, or booking. Newly assigned add-ons must be published, public, and owned by the same store; already assigned non-public add-ons may be retained for safe editing. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `product` — The product path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec replace(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkReplaceProductSAddOnsResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def replace(client, product, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkReplaceProductSAddOnsRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :put,
             "/v2/products/#{URI.encode(product, &URI.char_unreserved?/1)}/addons",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "replaceProductSAddOns"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkReplaceProductSAddOnsResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkReplaceProductSAddOnsResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
