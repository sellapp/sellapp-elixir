# This file is auto-generated by oagen. Do not edit.

defmodule SellApp.Wallet do
  @moduledoc """
  Operations for the Wallet API.
  """

  @doc """
  List customer wallets

  List customer wallet balances for the authenticated store. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `params` — query parameters: `:limit`, `:page`
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec list(SellApp.Client.t(), map(), keyword()) ::
          {:ok,
           SellApp.Page.t(
             SellApp.ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem.t()
           )}
          | {:error, SellApp.Error.error()}
  def list(client, params \\ %{}, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/wallets",
             params,
             Keyword.put(opts, :operation_id, "listCustomerWallets")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      fetch_next = fn cursor ->
        list(
          client,
          SellApp.Page.next_params(params, "page", cursor),
          Keyword.delete(opts, :with_response)
        )
      end

      result =
        SellApp.Page.from_map(
          body,
          "data",
          &SellApp.ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem.from_map/1,
          fetch_next,
          base_url: client.base_url,
          path: "/v2/wallets",
          pagination_param: "page"
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Retrieve a customer wallet

  Retrieve one current-store customer wallet by customer ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — The customer path parameter.
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec get(SellApp.Client.t(), String.t(), keyword()) ::
          {:ok, SellApp.SdkGetCustomerWalletResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def get(client, customer, opts \\ []) do
    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :get,
             "/v2/wallets/#{URI.encode(customer, &URI.char_unreserved?/1)}",
             %{},
             Keyword.put(opts, :operation_id, "getCustomerWallet")
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkGetCustomerWalletResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkGetCustomerWalletResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Adjust a customer wallet

  Add or subtract a nonzero amount from a customer wallet. Requires the wallet:write API ability and store wallet permission. Use signed integer cents and a nonblank note. Positive adjustments can create a wallet and follow the store's credit-expiration policy; negative adjustments need an existing eligible wallet and sufficient balance. Wallets must be enabled. Send idempotency_key in the body. Replaying the same key returns the original entry only for the same store, customer, staff actor, amount and trimmed note; changed input returns 422. Keep that identity unchanged across retries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — The customer path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec adjust(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkAdjustCustomerWalletResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def adjust(client, customer, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkAdjustCustomerWalletRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :post,
             "/v2/wallets/#{URI.encode(customer, &URI.char_unreserved?/1)}/adjustments",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "adjustCustomerWallet"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{kind: :model, module: SellApp.SdkAdjustCustomerWalletResponseValue200ApplicationJson},
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkAdjustCustomerWalletResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end

  @doc """
  Update a customer wallet status

  Freeze or activate an existing customer wallet. Requires wallet:write and the store wallet permission, with wallets enabled. Activating a frozen wallet expires eligible credits before returning, so its balance may decrease. Read balance_cents from the returned wallet. Repeating the current status leaves the wallet unchanged. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.

  ## Parameters

    * `customer` — The customer path parameter.
    * `params` — request body map
    * `opts` — per-request options (see `SellApp.Client.request/5`)
  """
  @spec update_status(SellApp.Client.t(), String.t(), map(), keyword()) ::
          {:ok, SellApp.SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson.t()}
          | {:error, SellApp.Error.error()}
  def update_status(client, customer, params \\ %{}, opts \\ []) do
    SellApp.Schema.validate_value!(
      params,
      %{kind: :model, module: SellApp.SdkUpdateCustomerWalletStatusRequestApplicationJson},
      :request,
      "$request.body"
    )

    with {:ok, body} <-
           SellApp.Client.request(
             client,
             :patch,
             "/v2/wallets/#{URI.encode(customer, &URI.char_unreserved?/1)}/status",
             params,
             Keyword.put(
               Keyword.put(opts, :request_body, true),
               :operation_id,
               "updateCustomerWalletStatus"
             )
           ) do
      raw_response = if match?(%SellApp.Response{}, body), do: body, else: nil
      body = if raw_response, do: raw_response.data, else: body

      SellApp.Schema.validate_value!(
        body,
        %{
          kind: :model,
          module: SellApp.SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson
        },
        :response,
        "$response"
      )

      result =
        SellApp.Cast.nested(
          body,
          &SellApp.SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson.from_map/1
        )

      {:ok, if(raw_response, do: %{raw_response | data: result}, else: result)}
    end
  end
end
